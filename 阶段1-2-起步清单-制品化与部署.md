# 阶段 1–2 起步清单：制品化、部署到测试环境、离线上生产彩排

> 前置：已完成《阶段 0 起步清单》——`gitea-ci` VM 里 Gitea + act_runner + Verdaccio 都在跑，smoke workflow 已绿。
>
> 本清单目标：拿一个**最小但真实**的 React+Vite + Express + Prisma 应用，在 VM 内跑通完整链路：
> **PR → CI → 合并 → 构建自包含制品 → 部署到测试环境 → 迁移测试库 → 健康检查**，
> 再起一个 **`prod-sim` VM**（断网）彩排「离线上生产 + 回滚」。
>
> 配套主文档：《软件开发与自动化部署运维平台-方案设计.md》。本清单是它 [第 10 节脚本模板](#) 的可执行落地版（monorepo 布局）。
>
> 约定：**【手动】** = 浏览器/交互操作；其余可复制粘贴。除 Part F 另起 VM 外，命令都在 `gitea-ci` VM 内执行。

---

## 目录

- [Part A — 创建最小真实应用仓库](#part-a--创建最小真实应用仓库)
- [Part B — Prisma 配置与首个迁移](#part-b--prisma-配置与首个迁移)
- [Part C — CI 工作流（PR 上跑）](#part-c--ci-工作流pr-上跑)
- [Part D — 制品化与部署脚本](#part-d--制品化与部署脚本)
- [Part E — 部署工作流：合并 main → 部到测试环境](#part-e--部署工作流合并-main--部到测试环境)
- [Part F — 起 prod-sim VM，彩排离线上生产 + 回滚](#part-f--起-prod-sim-vm彩排离线上生产--回滚)
- [验收清单](#-阶段-12-验收清单)

---

## Part A — 创建最小真实应用仓库

**【手动】** 先在 Gitea 新建一个仓库，例如 `myapp`（勾选初始化 README）。然后在 VM 里：

```bash
cd ~
git clone http://gitea-ci.orb.local:3000/<你的用户名>/myapp.git
cd myapp
mkdir -p backend/src backend/prisma backend/test frontend
```

### A.1 后端（Express + Prisma）

`backend/package.json` —— 注意 **`prisma` 放在 dependencies**（不是 devDependencies），这样离线生产机才能跑 `migrate deploy`：

```bash
cat > backend/package.json <<'JSON'
{
  "name": "myapp-backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node src/server.js",
    "lint": "echo 'lint ok'",
    "test": "node --test",
    "migrate:deploy": "prisma migrate deploy"
  },
  "dependencies": {
    "@prisma/client": "^6.5.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "ioredis": "^5.4.1",
    "prisma": "^6.5.0"
  }
}
JSON
```

> 版本号以当前稳定版为准；首次可 `cd backend && npm install <包>@latest` 让它自动写入精确版本。

`backend/src/server.js`：

```bash
cat > backend/src/server.js <<'JS'
require('dotenv').config();
const express = require('express');
const { PrismaClient } = require('@prisma/client');

const prisma = new PrismaClient();
const app = express();
app.use(express.json());

// 健康检查——部署脚本靠它判断新版本是否起来了
app.get('/health', (req, res) => res.json({ status: 'ok', ts: Date.now() }));

app.get('/api/notes', async (req, res) => {
  const notes = await prisma.note.findMany({ orderBy: { id: 'desc' } });
  res.json(notes);
});
app.post('/api/notes', async (req, res) => {
  const note = await prisma.note.create({ data: { text: String(req.body.text || '') } });
  res.json(note);
});

const port = process.env.PORT || 8080;
app.listen(port, () => console.log(`api on :${port}`));
JS
```

`backend/test/health.test.js`（让 `npm test` 有东西可跑）：

```bash
cat > backend/test/health.test.js <<'JS'
const test = require('node:test');
const assert = require('node:assert');
test('sanity', () => { assert.equal(1 + 1, 2); });
JS
```

### A.2 前端（React + Vite）

```bash
# 在 myapp 目录下用官方模板脚手架生成 frontend（走 Verdaccio/npmmirror）
npm create vite@latest frontend -- --template react
```

把 `frontend/src/App.jsx` 换成最小版（拉取 /api/notes）：

```bash
cat > frontend/src/App.jsx <<'JSX'
import { useEffect, useState } from 'react';
export default function App() {
  const [notes, setNotes] = useState([]);
  useEffect(() => {
    fetch('/api/notes').then(r => r.json()).then(setNotes).catch(() => {});
  }, []);
  return (
    <div style={{ fontFamily: 'sans-serif', padding: 24 }}>
      <h1>Notes</h1>
      <ul>{notes.map(n => <li key={n.id}>{n.text}</li>)}</ul>
    </div>
  );
}
JSX
```

（本地开发时想让前端直连后端，可在 `frontend/vite.config.js` 里加 `server.proxy['/api'] = 'http://localhost:8080'`；生产/测试环境是 Nginx 反代 `/api`，前端无需改。）

### A.3 仓库根：`.npmrc`、`ecosystem.config.js`

`.npmrc`（指向 Verdaccio，弱网稳定；可进仓库）：

```bash
cat > .npmrc <<'INI'
registry=http://localhost:4873/
fetch-retries=5
fetch-timeout=120000
INI
```

`ecosystem.config.js`（PM2 配置，制品里也带一份）：

```bash
cat > ecosystem.config.js <<'JS'
module.exports = {
  apps: [{
    name: 'app-api',
    script: 'backend/src/server.js',
    cwd: __dirname,
    instances: 2,
    exec_mode: 'cluster',          // cluster 支持零停机 reload
    env_test:       { NODE_ENV: 'test',       PORT: 8080 },
    env_production: { NODE_ENV: 'production',  PORT: 8080 },
  }],
};
JS
```

> 敏感值（DATABASE_URL / REDIS_URL）不写进这个进仓库的文件，由各机器本地 `.env` 在部署时注入（见 Part E）。

---

## Part B — Prisma 配置与首个迁移

`backend/prisma/schema.prisma`：

```bash
cat > backend/prisma/schema.prisma <<'PRISMA'
generator client {
  provider = "prisma-client-js"
  // 覆盖 arm64 与 amd64 两种 Ubuntu，制品即可在 VM(arm64) 与真机(amd64) 通用
  binaryTargets = ["native", "linux-arm64-openssl-3.0.x", "debian-openssl-3.0.x"]
}
datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
model Note {
  id        Int      @id @default(autoincrement())
  text      String
  createdAt DateTime @default(now())
}
PRISMA
```

> **关于 binaryTargets**：OrbStack 在 Apple Silicon 上是 **arm64** VM，用 `linux-arm64-openssl-3.0.x`；将来 amd64 真机用 `debian-openssl-3.0.x`。两个都列上，同一个制品就能跨架构跑。若某台机器是 Ubuntu 20.04（OpenSSL 1.1），把对应 target 换成 `...openssl-1.1.x`（用 `openssl version` 确认）。

生成首个迁移（连测试库；这一步在开发时做，迁移文件随代码提交）：

```bash
cd backend
export DATABASE_URL="postgresql://app_test:app_test_pw@127.0.0.1:5432/app_test"
npm install                       # 装依赖（走 Verdaccio）
npx prisma migrate dev --name init   # 生成 prisma/migrations/ 并应用到测试库
cd ..
```

提交初始代码：

```bash
git add .
git commit -m "feat: minimal express+prisma+vite app with CI"
git push
```

---

## Part C — CI 工作流（PR 上跑）

`.gitea/workflows/ci.yml`：

```bash
mkdir -p .gitea/workflows
cat > .gitea/workflows/ci.yml <<'YAML'
name: CI
on:
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest        # 对应 act_runner 注册时的 label
    steps:
      - uses: actions/checkout@v4

      - name: 后端依赖 + 生成 client
        working-directory: backend
        run: |
          npm ci
          npx prisma generate

      - name: 后端 lint + 测试
        working-directory: backend
        run: |
          npm run lint
          npm test

      - name: 前端依赖 + 构建验证
        working-directory: frontend
        run: |
          npm ci
          npm run build
YAML

git add .gitea/workflows/ci.yml
git commit -m "ci: PR checks"
git push
```

**验证**：新建一个分支改点东西、开 PR，Actions 里 `CI` 应跑绿。若首次 `npm ci` 慢或断，重跑一次即走 Verdaccio 缓存。

---

## Part D — 制品化与部署脚本

在仓库里建 `scripts/`，这些脚本由部署工作流调用：

```bash
mkdir -p scripts
```

`scripts/pack.sh` —— 生成自包含制品：

```bash
cat > scripts/pack.sh <<'SH'
#!/usr/bin/env bash
# 用法: scripts/pack.sh <sha>   在仓库根执行
set -euo pipefail
SHA="$1"
OUT="/opt/artifacts/app-${SHA}.tar.gz"
STAGE="$(mktemp -d)"
mkdir -p "$STAGE/backend" "$STAGE/frontend" /opt/artifacts

# 后端：只装生产依赖 + 生成 Prisma client/引擎
( cd backend && npm ci --omit=dev && npx prisma generate )
cp -r backend/src backend/package.json backend/package-lock.json \
      backend/prisma backend/node_modules "$STAGE/backend/"

# 前端：已构建的静态文件（deploy 工作流会先 build）
cp -r frontend/dist/* "$STAGE/frontend/"

# 运行配置
cp ecosystem.config.js "$STAGE/"

tar -C "$STAGE" -czf "$OUT" .
echo "制品已生成: $OUT"
rm -rf "$STAGE"
SH
chmod +x scripts/pack.sh
```

`scripts/health-check.sh`：

```bash
cat > scripts/health-check.sh <<'SH'
#!/usr/bin/env bash
# 用法: scripts/health-check.sh <url>
set -euo pipefail
URL="$1"
for i in $(seq 1 10); do
  if curl -fsS "$URL" >/dev/null; then echo "健康检查通过: $URL"; exit 0; fi
  sleep 2
done
echo "健康检查失败: $URL" >&2
exit 1
SH
chmod +x scripts/health-check.sh
```

`scripts/deploy-local-test.sh` —— 部署到本 VM 的测试环境：

```bash
cat > scripts/deploy-local-test.sh <<'SH'
#!/usr/bin/env bash
# 用法: scripts/deploy-local-test.sh <sha>
set -euo pipefail
SHA="$1"
BASE=/opt/app-test
REL="$BASE/releases/$SHA"

mkdir -p "$REL"
tar -C "$REL" -xzf "/opt/artifacts/app-${SHA}.tar.gz"

# 注入环境变量（DATABASE_URL / REDIS_URL 等）
set -a; source "$BASE/.env"; set +a

# 迁移测试库
( cd "$REL/backend" && ./node_modules/.bin/prisma migrate deploy )

# 切软链 + 零停机重启
ln -sfn "$REL" "$BASE/current"
cd "$BASE/current"
pm2 startOrReload ecosystem.config.js --env test --update-env
pm2 save
echo "已部署到测试环境: $SHA"
SH
chmod +x scripts/deploy-local-test.sh
```

准备测试环境目录、环境文件、Nginx（**一次性**，在 VM 里执行——注意用 `sudo` 且 chown 给 runner 用户 `gitea-runner`，因为 host 模式下工作流以它的身份运行）：

```bash
sudo mkdir -p /opt/app-test/releases /opt/artifacts
sudo chown -R gitea-runner:gitea-runner /opt/app-test /opt/artifacts

# 测试环境的本地环境文件（不进仓库）
sudo -u gitea-runner tee /opt/app-test/.env >/dev/null <<'ENV'
DATABASE_URL=postgresql://app_test:app_test_pw@127.0.0.1:5432/app_test
REDIS_URL=redis://127.0.0.1:6379
NODE_ENV=test
PORT=8080
ENV

# Nginx：静态托管前端 + 反代 /api
sudo tee /etc/nginx/sites-available/app-test >/dev/null <<'NGINX'
server {
    listen 8090;
    server_name _;
    root /opt/app-test/current/frontend;
    index index.html;
    location / { try_files $uri $uri/ /index.html; }
    location /api/ {
        proxy_pass http://127.0.0.1:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
NGINX
sudo ln -sf /etc/nginx/sites-available/app-test /etc/nginx/sites-enabled/
sudo nginx -t && sudo systemctl reload nginx
```

> 测试站点用 8090 端口，避免和 Gitea（3000）冲突。宿主机浏览器访问 **http://gitea-ci.orb.local:8090**。

---

## Part E — 部署工作流：合并 main → 部到测试环境

`.gitea/workflows/deploy-test.yml`：

```bash
cat > .gitea/workflows/deploy-test.yml <<'YAML'
name: Build & Deploy to Test
on:
  push:
    branches: [main]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: 构建前端
        working-directory: frontend
        run: |
          npm ci
          npm run build

      - name: 打包自包含制品
        run: ./scripts/pack.sh "${{ github.sha }}"

      - name: 部署到测试环境
        run: ./scripts/deploy-local-test.sh "${{ github.sha }}"

      - name: 健康检查
        run: ./scripts/health-check.sh "http://127.0.0.1:8080/health"
YAML

git add scripts .gitea/workflows/deploy-test.yml
git commit -m "ci: build artifact and deploy to test on main"
git push
```

**跑通它**：把上面这次 push（到 main）触发的 `Build & Deploy to Test` 看绿即可，或走正规流程——开 PR → CI 绿 → 合并 → 部署工作流触发。完成后：

```bash
# VM 内自检
pm2 list                                   # app-api 应 online
curl -s http://127.0.0.1:8080/health       # {"status":"ok",...}
curl -s http://127.0.0.1:8080/api/notes    # []（空数组）
```

宿主机浏览器打开 **http://gitea-ci.orb.local:8090** 应看到 Notes 页面。至此「合并 → 制品 → 测试部署 → 迁移 → 健康检查」在本地跑通。

---

## Part F — 起 prod-sim VM，彩排离线上生产 + 回滚

目的：在一台**断网**的机器上，只靠自包含制品完成部署，验证 air-gap 生产流程与回滚。

### F.1 【宿主机】创建 prod-sim VM

```bash
orb create ubuntu prod-sim
orb -m prod-sim   # 进入 prod-sim 的 shell，执行 F.2–F.4
```

### F.2 【prod-sim 内】一次性装运行时（趁现在有网）

```bash
sudo apt update
sudo apt install -y postgresql redis-server nginx openssh-server curl
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install -y nodejs
sudo npm i -g pm2

# 生产库
sudo -u postgres psql <<'SQL'
CREATE USER app_prod WITH PASSWORD 'app_prod_pw';
CREATE DATABASE app_prod OWNER app_prod;
SQL

# 部署用户 + 目录
sudo useradd --create-home --shell /bin/bash deployer
sudo mkdir -p /opt/app-prod/releases /opt/incoming /opt/db-backups
sudo chown -R deployer:deployer /opt/app-prod /opt/incoming /opt/db-backups

# 生产环境文件（只存在于生产机）
sudo -u deployer tee /opt/app-prod/.env >/dev/null <<'ENV'
DATABASE_URL=postgresql://app_prod:app_prod_pw@127.0.0.1:5432/app_prod
REDIS_URL=redis://127.0.0.1:6379
NODE_ENV=production
PORT=8080
ENV
```

放置 `deploy-prod.sh`（**预置在生产机**，含备份 + 迁移 + 回滚）：

```bash
sudo -u deployer tee /opt/app-prod/deploy-prod.sh >/dev/null <<'SH'
#!/usr/bin/env bash
# 位置: /opt/app-prod/deploy-prod.sh   用法: deploy-prod.sh <sha>
set -euo pipefail
SHA="$1"
BASE=/opt/app-prod
REL="$BASE/releases/$SHA"
set -a; source "$BASE/.env"; set +a

echo ">> 迁移前备份生产库"
pg_dump "$DATABASE_URL" | gzip > "/opt/db-backups/pre-${SHA}-$(date +%s).sql.gz"

echo ">> 解压制品（不联网）"
mkdir -p "$REL"
tar -C "$REL" -xzf "/opt/incoming/app-${SHA}.tar.gz"

echo ">> 数据库迁移（纯本地 SQL）"
( cd "$REL/backend" && ./node_modules/.bin/prisma migrate deploy )

echo ">> 切换并零停机重启"
PREV=$(readlink "$BASE/current" || true)
ln -sfn "$REL" "$BASE/current"
cd "$BASE/current"
pm2 startOrReload ecosystem.config.js --env production --update-env
pm2 save

echo ">> 健康检查（失败自动回滚）"
if ! "$BASE/health-check.sh" "http://127.0.0.1:8080/health"; then
  echo "!! 健康检查失败，回滚到 $PREV"
  [ -n "$PREV" ] && ln -sfn "$PREV" "$BASE/current" && pm2 startOrReload ecosystem.config.js --env production --update-env
  exit 1
fi
echo ">> 上生产完成: $SHA"
SH
sudo chmod +x /opt/app-prod/deploy-prod.sh

# health-check.sh 也放一份到生产机
sudo -u deployer tee /opt/app-prod/health-check.sh >/dev/null <<'SH'
#!/usr/bin/env bash
set -euo pipefail
URL="$1"
for i in $(seq 1 10); do
  if curl -fsS "$URL" >/dev/null; then echo "健康检查通过"; exit 0; fi
  sleep 2
done
echo "健康检查失败: $URL" >&2; exit 1
SH
sudo chmod +x /opt/app-prod/health-check.sh
```

Nginx（生产，端口 8090 便于宿主机访问 `http://prod-sim.orb.local:8090`）——同 Part D 的配置，`root` 改为 `/opt/app-prod/current/frontend`：

```bash
sudo tee /etc/nginx/sites-available/app-prod >/dev/null <<'NGINX'
server {
    listen 8090;
    server_name _;
    root /opt/app-prod/current/frontend;
    index index.html;
    location / { try_files $uri $uri/ /index.html; }
    location /api/ { proxy_pass http://127.0.0.1:8080; proxy_set_header Host $host; }
}
NGINX
sudo ln -sf /etc/nginx/sites-available/app-prod /etc/nginx/sites-enabled/
sudo nginx -t && sudo systemctl reload nginx
```

### F.3 打通 gitea-ci → prod-sim 的 SSH 部署通道

在 **prod-sim** 里授权 gitea-ci 的部署公钥。先在 **gitea-ci** VM 生成密钥：

```bash
# 【gitea-ci 内】以 runner 用户生成部署密钥
sudo -u gitea-runner ssh-keygen -t ed25519 -N '' -f /opt/act-runner/.ssh/deploy_key
sudo -u gitea-runner cat /opt/act-runner/.ssh/deploy_key.pub   # 复制这行公钥
```

```bash
# 【prod-sim 内】把上面的公钥加到 deployer
sudo -u deployer mkdir -p /home/deployer/.ssh
echo '<粘贴 gitea-ci 的 deploy_key.pub 内容>' | sudo -u deployer tee -a /home/deployer/.ssh/authorized_keys
sudo -u deployer chmod 700 /home/deployer/.ssh
sudo -u deployer chmod 600 /home/deployer/.ssh/authorized_keys
```

`scripts/promote-to-prod.sh`（**你在 gitea-ci 上手动运行**，把已构建好的制品送去生产）：

```bash
# 【gitea-ci 内】写进仓库
cat > scripts/promote-to-prod.sh <<'SH'
#!/usr/bin/env bash
# 用法: scripts/promote-to-prod.sh <sha>
set -euo pipefail
SHA="$1"
ART="/opt/artifacts/app-${SHA}.tar.gz"
PROD="deployer@prod-sim.orb.local"     # 真上生产时换成内网生产机地址
KEY="/opt/act-runner/.ssh/deploy_key"

echo ">> 传输制品到生产机（测试机推）"
rsync -avz -e "ssh -i $KEY -o StrictHostKeyChecking=accept-new" "$ART" "$PROD:/opt/incoming/"

echo ">> 远程执行离线部署"
ssh -i "$KEY" "$PROD" "/opt/app-prod/deploy-prod.sh $SHA"
SH
chmod +x scripts/promote-to-prod.sh
git add scripts/promote-to-prod.sh && git commit -m "ops: promote to prod script" && git push
```

### F.4 彩排：断网后部署

先确认制品存在（用 Part E 里已构建的某个 `<sha>`；`ls /opt/artifacts`）。然后模拟 air-gap：

```bash
# 【prod-sim 内】切断外网，但保留同网段（gitea-ci 仍可 rsync 过来）
GW=$(ip route | awk '/default/{print $3; exit}'); echo "记下网关: $GW"
sudo ip route del default
curl -m 5 https://registry.npmjs.org >/dev/null 2>&1 && echo "还有外网(异常)" || echo "外网已断 ✅"
```

```bash
# 【gitea-ci 内】手动 promote 一个已有制品
SHA=<粘贴 /opt/artifacts 里的某个 sha>
sudo -u gitea-runner ./scripts/promote-to-prod.sh "$SHA"
```

看到 `>> 上生产完成: <sha>` 即代表**在完全无外网的机器上，仅靠自包含制品完成了迁移 + 部署**。宿主机浏览器打开 **http://prod-sim.orb.local:8090** 验证。

彩排回滚：故意制造一次失败（比如临时改坏 health 路由再构建一个新制品 promote），观察 `deploy-prod.sh` 健康检查失败后自动切回上一版本。验证完恢复网络：

```bash
# 【prod-sim 内】恢复外网
sudo ip route add default via "$GW"    # 或直接重启 VM: 宿主机 orb restart prod-sim
```

---

## ✅ 阶段 1–2 验收清单

- [ ] `myapp` 仓库：后端 + 前端 + Prisma + `.gitea/workflows` 齐备
- [ ] PR 触发 `CI` 跑绿（lint/test/前端 build）
- [ ] `prisma migrate dev --name init` 生成了 `prisma/migrations/` 并提交
- [ ] 合并 main → `Build & Deploy to Test` 绿：制品生成、部署、迁移、健康检查全过
- [ ] 宿主机访问 `http://gitea-ci.orb.local:8090` 看到 Notes 页面
- [ ] `prod-sim` VM 运行时装好，`deploy-prod.sh`/`.env` 就位，SSH 通道打通
- [ ] **断网状态下** promote 成功，`http://prod-sim.orb.local:8090` 可访问
- [ ] 故意失败时 `deploy-prod.sh` 自动回滚到上一版本

全部打勾 = 你已在本地把「CI → 测试部署 → 离线上生产 → 回滚」整条链路验证完毕。之后扩展到公司内网/正式生产，就是把 `gitea-ci`（Part D/E）和 `prod-sim`（Part F）的步骤在真机上原样再跑一遍——**制品和 YAML 一字不改，只改各机 `.env` 里的地址**。

---

## 常见坑速查

| 现象 | 排查 |
|------|------|
| workflow 里 `./scripts/*.sh` 权限拒绝 | 确认脚本已 `chmod +x` 且提交（`git update-index --chmod=+x` 也可）|
| 部署工作流写 `/opt/app-test` 失败 | 确认 `/opt/app-test`、`/opt/artifacts` 已 `chown gitea-runner` |
| `prisma migrate deploy` 报引擎缺失 | `binaryTargets` 是否含该机架构；制品里 `backend/node_modules/.prisma` 是否带引擎 |
| prod-sim rsync 不通 | prod-sim 装了 `openssh-server`？公钥加对了？`prod-sim.orb.local` 能解析？|
| 断网后 rsync 也断 | 用 `ip route del default`（只断默认路由/外网），别整个关网卡；同网段 VM 仍可达 |
| pm2 reload 后仍旧版本 | 加 `--update-env`；必要时 `pm2 delete app-api` 再 start 一次 |
| 前端页面空白 | Nginx `root` 指向 `current/frontend`；`current` 软链是否指向最新 release |

---

*密码、用户名、仓库名、地址请按实际替换。任何一步卡住，把命令和报错贴给我，我帮你定位。*
