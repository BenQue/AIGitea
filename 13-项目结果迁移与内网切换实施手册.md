# 13 · 项目结果迁移与内网协作实施手册

> 状态：待执行（NewEmaint pilot override 更新于 2026-08-16）。本文迁移与同步的是「可重复构建的项目结果
> 与自动化部署脚本」，不是本地 Gitea 实例；不迁移本地 Issue、PR、评论、账号和 Secret。

## 0. 内网协作模型（权威分工，总纲）

公司内网当前不能使用 AI agent 开发或运维代码仓库。因此采用**双权威分工的持续协作模型**，
而不是一次性权威源切换：

- **开发权威 = 本地开发机**：Issue、判级、spec/plan、实现、测试、最终 PR 与人工合并全部
  发生在本地 Gitea；AI（Claude Code / Codex）只存在于开发侧。
- **部署权威 = 公司内网 Gitea**：持续同步开发侧仓库（源代码 + `.gitea/workflows/` +
  版本化部署脚本），在内网重新创建 PR、运行 CI/verification 并由人合并；具体 delivery profile
  决定是在公司隔离环境构建/测试，还是验证并消费开发侧交付的 exact bytes。内网不安装、
  不调用任何 AI。
- **传输通道 = GitHub 中继**：开发机把本地 Gitea 镜像到私有 GitHub；公司 `scm-ci` 经 HTTPS
  从 GitHub 拉取 allowlisted refs，走 [12-Linux §8](12-Linux-GitHub-Gitea-双服务器自动部署方案.md)
  的入站同步合同（`sync/github/<sha>` 分支 → 公司 Gitea PR → 内网人工合并 → 公司 `main`
  merge 后才可申请独立的确定性交付阶段）。`sync/` 子系统即为此而建。GitHub 只是搬运层，不进入公司信任链；
  公司侧 PR/CI/审批是内网自己的变更记录。
- **Issue 与提交永远留在开发机**：公司 Gitea 不承载新开发；生产/内网故障以脱敏证据包
  （06 §3）回流开发机建 Issue，修复经开发侧闭环后再同步入内网。
- **交付形态按项目 delivery profile 决定，流程不变量全平台一致**：docker-release/v2
  （NewEMaint 为参照实现）、PM2/tar.gz（受支持的选项路径）、Windows/IIS ZIP（本册 §9–§10）。
  不变量：不可变制品、测试与生产同字节晋级、部署前备份、精确 SHA 健康检查、可回滚、
  生产 script-only。有公司隔离测试环境的 profile 可由公司受控 builder 按同一 merge SHA 构建，且
  company test/prod 消费同一公司构建字节；要求跨环境字节一致的 profile 使用 docker-release offline
  bundle 经批准介质导入。两条路径不得混用测试结论。
- **阶段化路线**：公司侧基础（scm-ci 主机 + Gitea + Runner + sync timer + host profile）→
  逐仓同步接通（§6 bootstrap 基线 + 12-Linux §7/§8）→ 公司侧 PR CI（§8）→ profile 指定的
  隔离 test 或 exact-byte artifact-only gate → 生产 promotion（12-Linux §16 / 本册 §10）→
  运维验收与回退演练（§12、[14](14-Windows部署与迁移验收清单.md)）。

本册其余章节提供逐阶段操作；§11「最终权威切换」保留为**备选的彻底下线路径**（未来若决定
关停本地开发平台才执行），当前不采用。

### 0.1 NewEmaint pilot override：两台公司 VM + 本地 test

NewEmaint 不走“公司再建一台 `appserver-test` 并重新 build”的默认假设：

| 位置 | role | 当前合同 |
|---|---|---|
| 公司 VM 1 `gitea-ci` | `scm-ci` | 公司 Gitea、GitHub one-shot 入站、company PR/CI、act_runner、Registry/cache、artifact-only verification、受控编排；不得运行业务 runtime/DB |
| 本地 OrbStack NewEmaint DockerLab | `appserver-test` | 对 exact `docker-release/v2` bytes 做非生产 deploy/migration/health/rollback；不是 company/live evidence |
| 公司 VM 2 `appserver` | `appserver-prod` | NewEmaint runtime、PostgreSQL、Nginx、fixed production target；不得安装 Gitea/通用 Runner/源码 builder |

开发侧准备 versioned + checksum-pinned operator bundle，并把已在本地 DockerLab 验证的 exact
`docker-release/v2` bytes 经受控 offline transport 搬入公司。公司 `scm-ci` 只做 artifact-only
verification；公司要求内网重建且没有隔离测试环境时固定 `BLOCKED`，不同 bytes 不得沿用本地测试
证明。公司重新创建 PR/CI/人工合并，但这不把新构建 bytes 自动变成已测试 release。

所有公司动作必须按 [`company-delivery/runbook.md`](company-delivery/runbook.md) 的 Stage 00–110
每次只批准一个阶段；首次验收时 sync timer、Actions auto deploy 与 production gate 保持
disabled/inactive。本册当前没有公司执行证据，两台公司 VM、Gitea/Runner/Registry、backup/restore、
真实 handoff、AppServer 与 production 全部为 `NOT RUN`。

## 1. 拓扑

持续协作模式（当前目标）：

```text
Mac -> 本地 Gitea(开发权威) -> GitHub 私有中继
  |                                |
  | NewEmaint exact offline bytes  | 公司 scm-ci one-shot 拉取 allowlisted refs
  v                                v
本地 OrbStack DockerLab       公司 VM 1 gitea-ci/Gitea(部署权威)
 appserver-test 验证                 |
                                    | artifact-only PASS + 独立人工 gate
                                    v
                          公司 VM 2 appserver/appserver-prod
```

本地 Gitea 长期保留为开发权威；公司 Gitea 的 Issue/PR 功能只用于入站 sync PR 与部署审计，
不承载新需求。

## 2. 迁移与重建边界

### 2.1 迁移

- `main` 的完整 Git 历史。
- 明确仍需保留的发布 tags。
- 必须保留的长期分支；临时 feature/change 分支默认不迁移。
- Git LFS 对象和 submodule 定义。
- `.gitea/workflows/`。
- 源代码、测试、migration、部署脚本和配置模板。
- npm/NuGet lock files。
- `docs/` 中已经形成的技术结果。
- handoff manifest 与最终 commit SHA。

### 2.2 不迁移

- 本地 Issue、PR、评论、labels、milestones。
- 本地用户、PAT、SSH key、OAuth、SMTP 和 webhook。
- Runner 注册文件、registration token、Actions Secret/Variable。
- GitHub mirror 配置和 GitHub token。
- 本地 `.env`、数据库、证书私钥、缓存和日志。
- 未进入项目 approved offline bundle 合同的 Mac/OrbStack 应用制品。NewEmaint 的 exact
  `docker-release/v2` bytes 是受 checksum-pinned handoff 管理的例外 transport，不是随意复制原型制品。
- 原型部署状态和临时测试数据。

### 2.3 在公司重新建立

- 仓库权限、团队、protected branch 和 required status context。
- Windows/Linux Runner 和内部 Actions。
- npm/NuGet 依赖缓存。
- Gitea Generic Package Registry 发布凭据。
- OpenSSH 测试通道。
- SMB、AD、Kerberos、WinRM 和 JEA。
- 测试/生产配置、Secret、TLS 和 PostgreSQL 账号。
- 公司 Gitea、Packages 和生产数据备份。

## 3. 迁移工件

迁移目录建议包含：

```text
handoff/
├── MIGRATION-MANIFEST.yml
├── repository.bundle          # 可选，完整离线 Git 归档
├── refs-before.txt
├── lfs-inventory.txt
├── software-inventory.md
├── verification-results.md
└── SHA256SUMS
```

`MIGRATION-MANIFEST.yml` 示例：

```yaml
handoff_id: MIG-MYAPP-YYYYMMDD
source_authority: local-gitea
source_repository: admin/myapp
source_change_prefix: MYA
source_tag: prototype-handoff-v1
source_commit_sha: FULL_SHA
target_authority: company-gitea
target_repository: software/myapp
target_change_prefix: PRD
architecture_profile_id: linux-node-postgres-v1   # 或该项目实际 profile
architecture_catalog_revision: "2026.08.3"
architecture_lock_sha256: LOCK_SHA256             # .aisoft/architecture.lock.json 自校验值
includes:
  - main-history
  - release-tags
  - workflows
  - deployment-scripts
  - migrations
  - tests
  - lock-files
excludes:
  - issues-and-pull-requests
  - users-and-secrets
  - prototype-artifacts
```

清单只存标识和校验信息，不存 token、密码或私钥。带 `.aisoft/architecture.json` 的项目必须
填三个 architecture 字段（07 §10：迁移 handoff 只引用 `profile_id`、`catalog_revision` 和
lock checksum）；无架构声明的旧项目在补齐声明前不得进入内网正式通道。

## 4. Phase A：原型冻结

### A1. 收敛工作

- 合并所有需要进入正式基线的 PR。
- 关闭或记录不进入基线的实验分支。
- 等待 Gitea Actions 结束。
- 确认默认分支构建、测试、Windows 测试部署和回滚证据。
- 确认仓库没有 `.env`、token、数据库文件或私钥。

### A2. 创建 handoff tag

```bash
git switch main
git pull --ff-only origin main
git status --short
git tag -a prototype-handoff-v1 -m "Validated prototype handoff baseline"
git push origin prototype-handoff-v1
```

只有 `git status --short` 为空且目标 commit 已完成验证时才创建 tag。

### A3. 保存原型历史

可生成 Git bundle 作为离线归档：

```bash
git bundle create repository.bundle --all
git bundle verify repository.bundle
```

本地 Gitea 还应进行一次一致性备份，保留未迁移的 Issue/PR 历史。该备份不进入 GitHub或公司正式 Gitea，只作为加密归档。

## 5. Phase B：公司内网准备

### B1. 新建普通仓库

在公司 Gitea 新建空的普通仓库：

```text
software/myapp
```

约束：

- 不是 Pull Mirror。
- 不配置 GitHub Push Mirror。
- Actions 先保持关闭或没有可运行 Runner。
- 初始阶段不保存生产 Secret。
- 使用公司内部 FQDN 和 TLS。

### B2. 建设身份与权限

- 先生成目标 Gitea inventory/mapping 并单独批准；不能扫描后自动接管所有仓库。
- 默认创建 private 仓库。只有逻辑仓库 `aisoft-platform`、`myapp`、`smoke-test` 或未来经独立
  平台 Issue/PR 批准的测试仓库可进入 public allowlist；内部应用保持 private。
- 公司个人/AD reviewer 账号负责审核和最终合并，merge allowlist 不包含任何 service account。
- 公司环境重新创建非 site-admin 的 platform manager，在显式受管仓库为 Admin；audit/mutation
  credential 分离，不复制本地 PAT。
- 每个项目重新创建唯一 project agent，只对自己的仓库 Write；不能使用一个共享 CI/Agent
  账号写多个项目，也不能进入 push/force-push/merge allowlist。
- CI service account 只读源码、写 Package 所需最小权限；若与 project agent 职责不同就继续分离。
- Windows Runner 使用独立服务账号。
- 每项目测试 deploy account 只访问本项目测试目标，不复用 Gitea PAT。
- 每项目生产部署 AD 组只访问部署控制端和本项目 JEA Endpoint；AI/通用 Runner 无生产登录权。

## 6. Phase C：一次性 Git 结果迁移

### C1. 推荐传输方式

目标是迁移默认分支完整历史和需要的 tags，而不是把所有临时 refs 原样复制到正式仓库：

```bash
git clone --bare <local-gitea-repository-url> myapp-transfer.git
cd myapp-transfer.git
git fsck --full
git remote add company <company-gitea-repository-url>
git push company refs/heads/main:refs/heads/main
git push company --tags
```

需要保留的其他长期分支逐个显式推送。只有确定目标为空并确实要复制所有 refs 时才使用 `git push --mirror`；它会删除或覆盖目标 refs，不作为默认命令。

### C2. Git LFS

如仓库使用 Git LFS：

```bash
git lfs fetch --all origin
git lfs push --all company
```

对 LFS 数量和总大小生成迁移前后清单。

### C3. Submodule 与硬编码地址

迁移前搜索并处理：

```text
*.orb.local
192.168.139.*
本地 Gitea URL
本地 Verdaccio URL
Mac 绝对路径
个人用户名
```

Submodule 如果仍需使用，必须先在公司 Gitea 建立对应目标，再更新 `.gitmodules`。

## 7. Phase D：结果整理与 Change ID 切换

原型 Change ID 使用项目三字符前缀，例如 `MYA-0001`；正式环境从 `PRD-0001` 独立开始。
注意（07 §4 限定）：现有 runtime 只接受 `change/N-short-description`（证据兼容 legacy
`change/N`），`change/PRD-NNNN` 尚未实现——启用正式 Change ID 格式必须先有独立的平台
实现变更；在此之前公司侧变更记录以入站 sync PR 与部署审计承载。

本地决策文档如需进入正式仓库，建议移动到：

```text
docs/legacy/prototype/
```

新的正式变更使用：

```text
docs/changes/PRD-0001/
change/PRD-0001
```

在迁移基线中增加一份 `docs/migration/MIGRATION-MANIFEST.yml`，建立 handoff tag、source SHA、原型前缀和正式前缀的关系。原型编号不转换成正式编号，确有来源时在新文档中填写：

```yaml
source_change_id: MYA-0008
```

## 8. Phase E：重新配置 Gitea 与 CI

必须在启用 Actions 前完成：

- 建立组织、团队和最小权限，并读回 platform manager/project agent 不具有跨项目写权限。
- 按迁移 mapping 读回 private/public；匿名访问必须只能读取批准的 public 例外，不能读取任何
  internal application。
- 保护 `main`，禁止直接 push。
- 禁止 force push；merge allowlist 只能包含人工 reviewer，并阻止管理员 merge override 旁路。
- 配置 PR required status context。
- 将 workflow 依赖的 Actions 镜像到内网或改为明确的内网 URL。
- 注册 Windows x64 Runner；Gitea 与 Runner 分机。
- 建立 NuGet/npm 缓存并完成离线命中验证。
- 重新创建 Actions Secret/Variable，禁止从本地导入旧 token。
- 配置公司 SMTP、AD/LDAP、TLS 和备份。

workflow 中环境差异只通过 Variables/Secrets 表达，例如：

```text
RUNNER_LABEL
PACKAGE_REGISTRY_URL
TEST_HOST
TEST_SSH_USER
TEST_APP_ROOT
```

生产凭据不进入普通 build job。

## 9. Phase F：Windows / 有隔离测试环境 profile 的内网重新构建与测试

本节不适用于 NewEmaint pilot。NewEmaint 公司侧没有第三台 `appserver-test` VM，不得在公司重建后沿用
本地测试证明；它只按 §0.1 与 `company-delivery/runbook.md` 搬运并验证 exact
`docker-release/v2` bytes。公司要求内网重建且无隔离测试环境时结论为 `BLOCKED`。

从 `prototype-handoff-v1` 对应 commit 在 Windows x64 Runner 重新构建：

```text
npm ci
→ React test/build
→ dotnet restore/test/publish
→ 空 PostgreSQL migration
→ migration bundle
→ manifest + ZIP + SHA256
→ Gitea Generic Package Registry
```

原型阶段生成的 ZIP 不得直接晋级。内网生成的制品至少部署测试环境两次，并完成一次故意失败回滚，证据写入 [14](14-Windows部署与迁移验收清单.md)。

## 10. Phase G：生产通道建设

按 [12](12-Windows平台自动部署方案.md) 建立：

1. 部署控制端从 Gitea Package Registry 获取批准制品。
2. 通过 SMB 将 ZIP 和 SHA256 写入生产服务器本地 `incoming`。
3. 使用 FQDN 和 Kerberos 连接 WinRM JEA Endpoint。
4. JEA 仅调用版本化 PowerShell module 中的固定函数。
5. 完成 PostgreSQL 备份、计划停机、migration、junction 切换、启动和健康检查。
6. 保存 JEA transcript、部署记录和备份 ID。

不得让通用 Runner 直接成为生产本地管理员。

## 11. Phase H：最终权威切换（备选路径，当前不采用）

> 本节仅在未来决定彻底下线本地开发平台时执行。§0 的持续协作模型下，本地 Gitea 长期保留
> 开发权威，不做冻结与 remote 切换。

### H1. 最终冻结

- 宣布本地写入冻结窗口。
- 完成最后一次 `main` 和 tag 对账。
- 停止本地项目 Actions、analyzer 和 Loop。
- 保存最终 Git bundle 和本地 Gitea 备份。
- 在公司仓库完成最后一次 fast-forward 同步。

### H2. 对账

```bash
git rev-parse refs/heads/main
git ls-remote <local-url> refs/heads/main
git ls-remote <company-url> refs/heads/main
git ls-remote <company-url> refs/tags/prototype-handoff-v1
```

四处 SHA 必须符合 manifest。LFS、submodule、workflow 和 lock file 也必须单独检查。

### H3. Mac remote 切换

在 Mac 正常工作克隆中：

```bash
git remote rename origin prototype
git remote add origin <company-gitea-repository-url>
git fetch origin
git branch --set-upstream-to=origin/main main
```

在本备选彻底下线路径中，随后创建小型文档分支和 PR，验证 push、PR、CI、邮件和权限；验证
完成后，公司 Gitea 才宣布为唯一权威源。

## 12. 回退策略

### 12.1 §11 备选切换的简单回退窗口

仅在已单独批准并启动 §11、且公司 Gitea 尚未接受新开发提交、Issue 或 PR 时适用：

```text
停止公司 Actions
→ 隔离公司暂存仓库
→ 解除本地冻结
→ 修正方案后重新演练
```

### 12.2 越过切换点后（仅备选的彻底切换路径适用）

若执行了 §11 的最终权威切换：公司 Gitea 一旦接受新工作，本地副本不得重新成为权威源。
持续协作模式下不存在该切换点——公司侧故障恢复使用公司自身备份，开发权威不受影响；
公司侧同步/部署失败的回退是「停公司 Actions → 修复后重新 reconcile」，开发侧照常工作。

### 12.3 应用发布回退

- 制品校验、备份或 migration 前置检查失败：保持当前 release。
- migration 后启动/健康失败：按兼容合同回切应用 release。
- 数据库需要恢复：停止发布并人工确认，不由普通健康失败自动触发。

## 13. 持续协作投运完成定义

只有项目 delivery profile 的适用条件全部满足，当前采用的双权威持续协作链路才完成投运：

- 迁移清单、handoff tag 和 SHA256 已归档。
- 公司 `main` 与 handoff SHA 一致。
- workflow、LFS、submodule 和 lock file 校验完成。
- 本地 Gitea 保持开发权威与正常写入；Mac `origin` 仍指向本地 Gitea。
- 私有 GitHub 搬运中继已验证 exact ref，且不承载公司 Secret 或审批权威。
- 公司 Gitea 已从 allowlisted ref 完成一次 sync PR、内网人工合并与审计读回。
- 内网 Runner 完成该 profile 要求的干净 CI；若 profile 允许公司 build，还必须从干净状态构建成功。
- 同一制品通过 profile 指定的隔离测试部署和故意失败回滚；NewEmaint 使用本地 DockerLab exact-byte
  evidence，并在公司做 artifact-only verification，不要求或伪造第三台公司 test VM。
- PostgreSQL 备份完成真实恢复演练。
- SMB、Kerberos、WinRM、JEA 和 transcript 验证通过。
- 内网故障的脱敏证据包已演练回流本地 Issue，并能从修复后的同一 ref 重新同步。
- 公司备份恢复路径已验证。

只有未来单独批准并执行 §11 的备选彻底下线路径时，完成定义才改为“Mac 通过公司 Gitea
完成开发 PR、本地项目停止正式写入、公司 Gitea 成为唯一权威源”；这些不是当前投运条件。

所有未执行项保持 `NOT RUN`，不得把设计或手工试验描述为验收通过。
