# 13 · 项目结果迁移与内网切换实施手册

> 状态：待执行。本文迁移的是“可重复构建的项目结果”，不是本地 Gitea 实例；不迁移本地 Issue、PR、评论、账号和 Secret。

## 1. 迁移目标

原型阶段：

```text
Mac -> 本地 Gitea -> GitHub 镜像备份
```

正式切换后：

```text
Mac -> 公司内网 Gitea -> 内网 CI/制品/测试/生产
```

GitHub 与公司 Gitea 无网络、镜像或发布关系。本地 Gitea 在切换后只作为原型历史或新平台实验沙盒。

## 2. 迁移与重建边界

### 2.1 迁移

- `main` 的完整 Git 历史。
- 明确仍需保留的发布 tags。
- 必须保留的长期分支；临时 feature/change 分支默认不迁移。
- Git LFS 对象和 submodule 定义。
- `.gitea/workflows/`。
- 源代码、测试、migration、部署脚本和配置模板。
- npm/NuGet lock files。
- `docs/` 中已经形成的技术结果。
- handoff manifest 与最终 commit SHA。

### 2.2 不迁移

- 本地 Issue、PR、评论、labels、milestones。
- 本地用户、PAT、SSH key、OAuth、SMTP 和 webhook。
- Runner 注册文件、registration token、Actions Secret/Variable。
- GitHub mirror 配置和 GitHub token。
- 本地 `.env`、数据库、证书私钥、缓存和日志。
- Mac/OrbStack 构建的应用制品。
- 原型部署状态和临时测试数据。

### 2.3 在公司重新建立

- 仓库权限、团队、protected branch 和 required status context。
- Windows/Linux Runner 和内部 Actions。
- npm/NuGet 依赖缓存。
- Gitea Generic Package Registry 发布凭据。
- OpenSSH 测试通道。
- SMB、AD、Kerberos、WinRM 和 JEA。
- 测试/生产配置、Secret、TLS 和 PostgreSQL 账号。
- 公司 Gitea、Packages 和生产数据备份。

## 3. 迁移工件

迁移目录建议包含：

```text
handoff/
├── MIGRATION-MANIFEST.yml
├── repository.bundle          # 可选，完整离线 Git 归档
├── refs-before.txt
├── lfs-inventory.txt
├── software-inventory.md
├── verification-results.md
└── SHA256SUMS
```

`MIGRATION-MANIFEST.yml` 示例：

```yaml
handoff_id: MIG-MYAPP-20260717
source_authority: local-gitea
source_repository: admin/myapp
source_change_prefix: MYA
source_tag: prototype-handoff-v1
source_commit_sha: FULL_SHA
target_authority: company-gitea
target_repository: software/myapp
target_change_prefix: PRD
includes:
  - main-history
  - release-tags
  - workflows
  - deployment-scripts
  - migrations
  - tests
  - lock-files
excludes:
  - issues-and-pull-requests
  - users-and-secrets
  - prototype-artifacts
```

清单只存标识和校验信息，不存 token、密码或私钥。

## 4. Phase A：原型冻结

### A1. 收敛工作

- 合并所有需要进入正式基线的 PR。
- 关闭或记录不进入基线的实验分支。
- 等待 Gitea Actions 结束。
- 确认默认分支构建、测试、Windows 测试部署和回滚证据。
- 确认仓库没有 `.env`、token、数据库文件或私钥。

### A2. 创建 handoff tag

```bash
git switch main
git pull --ff-only origin main
git status --short
git tag -a prototype-handoff-v1 -m "Validated prototype handoff baseline"
git push origin prototype-handoff-v1
```

只有 `git status --short` 为空且目标 commit 已完成验证时才创建 tag。

### A3. 保存原型历史

可生成 Git bundle 作为离线归档：

```bash
git bundle create repository.bundle --all
git bundle verify repository.bundle
```

本地 Gitea 还应进行一次一致性备份，保留未迁移的 Issue/PR 历史。该备份不进入 GitHub或公司正式 Gitea，只作为加密归档。

## 5. Phase B：公司内网准备

### B1. 新建普通仓库

在公司 Gitea 新建空的普通仓库：

```text
software/myapp
```

约束：

- 不是 Pull Mirror。
- 不配置 GitHub Push Mirror。
- Actions 先保持关闭或没有可运行 Runner。
- 初始阶段不保存生产 Secret。
- 使用公司内部 FQDN 和 TLS。

### B2. 建设身份与权限

- 公司个人/AD 账号负责审核和合并。
- CI service account 只读源码、写 Package 所需最小权限。
- Windows Runner 使用独立服务账号。
- 测试 deploy account 只访问测试目标。
- 生产部署 AD 组只访问部署控制端和 JEA Endpoint。

## 6. Phase C：一次性 Git 结果迁移

### C1. 推荐传输方式

目标是迁移默认分支完整历史和需要的 tags，而不是把所有临时 refs 原样复制到正式仓库：

```bash
git clone --bare <local-gitea-repository-url> myapp-transfer.git
cd myapp-transfer.git
git fsck --full
git remote add company <company-gitea-repository-url>
git push company refs/heads/main:refs/heads/main
git push company --tags
```

需要保留的其他长期分支逐个显式推送。只有确定目标为空并确实要复制所有 refs 时才使用 `git push --mirror`；它会删除或覆盖目标 refs，不作为默认命令。

### C2. Git LFS

如仓库使用 Git LFS：

```bash
git lfs fetch --all origin
git lfs push --all company
```

对 LFS 数量和总大小生成迁移前后清单。

### C3. Submodule 与硬编码地址

迁移前搜索并处理：

```text
*.orb.local
192.168.139.*
本地 Gitea URL
本地 Verdaccio URL
Mac 绝对路径
个人用户名
```

Submodule 如果仍需使用，必须先在公司 Gitea 建立对应目标，再更新 `.gitmodules`。

## 7. Phase D：结果整理与 Change ID 切换

原型 Change ID 使用项目三字符前缀，例如 `MYA-0001`；正式环境从 `PRD-0001` 独立开始。

本地决策文档如需进入正式仓库，建议移动到：

```text
docs/legacy/prototype/
```

新的正式变更使用：

```text
docs/changes/PRD-0001/
change/PRD-0001
```

在迁移基线中增加一份 `docs/migration/MIGRATION-MANIFEST.yml`，建立 handoff tag、source SHA、原型前缀和正式前缀的关系。原型编号不转换成正式编号，确有来源时在新文档中填写：

```yaml
source_change_id: MYA-0008
```

## 8. Phase E：重新配置 Gitea 与 CI

必须在启用 Actions 前完成：

- 建立组织、团队和最小权限。
- 保护 `main`，禁止直接 push。
- 配置 PR required status context。
- 将 workflow 依赖的 Actions 镜像到内网或改为明确的内网 URL。
- 注册 Windows x64 Runner；Gitea 与 Runner 分机。
- 建立 NuGet/npm 缓存并完成离线命中验证。
- 重新创建 Actions Secret/Variable，禁止从本地导入旧 token。
- 配置公司 SMTP、AD/LDAP、TLS 和备份。

workflow 中环境差异只通过 Variables/Secrets 表达，例如：

```text
RUNNER_LABEL
PACKAGE_REGISTRY_URL
TEST_HOST
TEST_SSH_USER
TEST_APP_ROOT
```

生产凭据不进入普通 build job。

## 9. Phase F：内网重新构建与测试

从 `prototype-handoff-v1` 对应 commit 在 Windows x64 Runner 重新构建：

```text
npm ci
→ React test/build
→ dotnet restore/test/publish
→ 空 PostgreSQL migration
→ migration bundle
→ manifest + ZIP + SHA256
→ Gitea Generic Package Registry
```

原型阶段生成的 ZIP 不得直接晋级。内网生成的制品至少部署测试环境两次，并完成一次故意失败回滚，证据写入 [14](14-Windows部署与迁移验收清单.md)。

## 10. Phase G：生产通道建设

按 [12](12-Windows平台自动部署方案.md) 建立：

1. 部署控制端从 Gitea Package Registry 获取批准制品。
2. 通过 SMB 将 ZIP 和 SHA256 写入生产服务器本地 `incoming`。
3. 使用 FQDN 和 Kerberos 连接 WinRM JEA Endpoint。
4. JEA 仅调用版本化 PowerShell module 中的固定函数。
5. 完成 PostgreSQL 备份、计划停机、migration、junction 切换、启动和健康检查。
6. 保存 JEA transcript、部署记录和备份 ID。

不得让通用 Runner 直接成为生产本地管理员。

## 11. Phase H：最终权威切换

### H1. 最终冻结

- 宣布本地写入冻结窗口。
- 完成最后一次 `main` 和 tag 对账。
- 停止本地项目 Actions、analyzer 和 Loop。
- 保存最终 Git bundle 和本地 Gitea 备份。
- 在公司仓库完成最后一次 fast-forward 同步。

### H2. 对账

```bash
git rev-parse refs/heads/main
git ls-remote <local-url> refs/heads/main
git ls-remote <company-url> refs/heads/main
git ls-remote <company-url> refs/tags/prototype-handoff-v1
```

四处 SHA 必须符合 manifest。LFS、submodule、workflow 和 lock file 也必须单独检查。

### H3. Mac remote 切换

在 Mac 正常工作克隆中：

```bash
git remote rename origin prototype
git remote add origin <company-gitea-repository-url>
git fetch origin
git branch --set-upstream-to=origin/main main
```

随后创建小型文档分支和 PR，验证 push、PR、CI、邮件和权限。验证完成后，公司 Gitea 才宣布为唯一权威源。

## 12. 回退策略

### 12.1 简单回退窗口

公司 Gitea 尚未接受新提交、Issue 或 PR 前：

```text
停止公司 Actions
→ 隔离公司暂存仓库
→ 解除本地冻结
→ 修正方案后重新演练
```

### 12.2 越过切换点后

公司 Gitea 一旦接受新工作，本地副本不得重新成为权威源。故障恢复使用公司 Gitea 数据库、repositories、LFS、Packages 和配置备份。

### 12.3 应用发布回退

- 制品校验、备份或 migration 前置检查失败：保持当前 release。
- migration 后启动/健康失败：按兼容合同回切应用 release。
- 数据库需要恢复：停止发布并人工确认，不由普通健康失败自动触发。

## 13. 完成定义

只有以下条件全部满足，迁移才完成：

- 迁移清单、handoff tag 和 SHA256 已归档。
- 公司 `main` 与 handoff SHA 一致。
- workflow、LFS、submodule 和 lock file 校验完成。
- 内网 Runner 从干净状态构建成功。
- 同一制品通过两次测试部署和一次故意失败回滚。
- PostgreSQL 备份完成真实恢复演练。
- SMB、Kerberos、WinRM、JEA 和 transcript 验证通过。
- Mac 已通过公司 Gitea 完成一次 PR。
- 公司备份恢复路径已验证。
- 本地项目仓库已停止正式写入。

所有未执行项保持 `NOT RUN`，不得把设计或手工试验描述为验收通过。
