# 阶段 2.5 起步清单：在 VM 内用 Claude Code 接入 AI Agent

> 前置：阶段 0–2 已跑通——Gitea、act_runner、测试环境、`prod-sim` 离线彩排都 OK。
>
> 本阶段目标：让「**issue → AI 分析 → 人工批准 → 自动开发 → 开 PR**」闭环起来，接上你已有的 CI/部署流水线，形成完整的一条龙。
>
> **架构落点（已确认）**：所有 agent 逻辑跑在 **gitea-ci VM 内的 Claude Code**。原因：Claude 的桌面沙盒解析不了 `.orb.local`、够不到你本地 Gitea；而 VM 内的 Claude Code 能直连 Gitea、有现成开发环境。附带好处——**loop 常驻 VM，你电脑关不关机都不影响**。
>
> 版本核准：Claude Code 安装包 `@anthropic-ai/claude-code`；headless 用 `claude -p` + `--permission-mode`；自动读仓库根 `CLAUDE.md`。
>
> **⚠️ 口径已被《阶段 2.6》更新**：本清单的 agent 脚本（analyze/implement/poll）、分支命名（`feat/issue-N`）、标签（`awaiting-approval`）已被 2.6 取代——以 2.6 为准（分支 `spec/N`+`change/N`，标签 `needs-analysis/awaiting-triage/spec-drafting/spec-review/approved/pr-open/deployed`）。**2.5 仅保留一次性基础设施**：ci-bot 账号、coder 用户与 git 凭据、`~/.agent.env` 模板、systemd 定时器。目标仓库统一为 `rsdesign-new`。

---

## 全景：谁做什么

```
用户提 issue
   │  ①【人工/触发】给 issue 打标签 needs-analysis
   ▼
VM 内 poller（systemd 定时，每 15 分钟）
   │  发现 needs-analysis → 跑 analyze.sh
   ▼
Claude Code（只读）分析 → 贴「开发方案」评论 → 标签改 awaiting-approval
   │
   ▼  ②【人工闸门1】你看方案，满意就把标签改成 approved
   │
VM 内 poller 发现 approved → 跑 implement.sh
   │
   ▼
Claude Code（可改文件/跑测试）按方案实现 → npm test 通过
   │  脚本：建 feat/issue-N 分支 → commit → push（ci-bot）→ 开 PR → 标签改 pr-open
   ▼
Gitea Actions 跑 CI（阶段 1 的 ci.yml）
   │
   ▼  ③【人工闸门2】你 review PR → Merge
   │
合并触发部署（阶段 2 的 deploy-test.yml）→ 测试环境
   │
   ▼  ④【人工】验证测试环境 → 手动 promote 上生产（阶段 2 Part F）
```

三道人工闸门：**批准方案 / 合并 PR / 批准上生产**。AI 只做分析和写代码，绝不合并、绝不部署。

---

## Part A — 建 ci-bot 账号与最小权限 token

**【手动】在 Gitea 里：**

1. Site Administration → 用户管理 → 新建用户 `ci-bot`（若阶段 0 已建可跳过）。
2. 把 `ci-bot` 加为 `rsdesign-new` 仓库的 Collaborator，权限 **Write**（能推分支、开 PR；**不给** Admin）。
3. 用 `ci-bot` 登录 → Settings → Applications → 生成 Token，**Scopes 只勾**：
   - `write:issue`（读写 issue、评论、标签）
   - `write:repository`（推分支、开 PR）
   - 其余一律不勾（无 admin、无 org）。
4. 复制 token 备用（下面填进 `.agent.env`）。

**【手动】给 main 上分支保护**（关键——让 ci-bot 无法自己合并）：
仓库 Settings → Branches → 保护 `main`：要求 PR、要求 CI 通过才能合并、禁止直接 push。这样 agent 顶多提 PR，合并权只在你手里。

**【手动】建 4 个流程标签**：仓库 Issues → Labels，新建 `needs-analysis`、`awaiting-approval`、`approved`、`pr-open`（颜色随意）。

---

## Part B — 仓库根放 CLAUDE.md（agent 的"规范"）

Claude Code 启动时会自动读仓库根的 `CLAUDE.md`——这就是你要的"编码项目规范的专属技能"，用 Claude Code 原生机制实现。在 VM 的 myapp 克隆里执行：

```bash
cat > CLAUDE.md <<'MD'
# 项目规范（Claude Code 自动读取）

## 结构
- backend/  : Express + Prisma API（Node 20, CommonJS）
- frontend/ : React + Vite（构建产物在 frontend/dist）
- Prisma schema: backend/prisma/schema.prisma

## 命令
- 后端测试:   cd backend && npm test
- 前端构建:   cd frontend && npm run build
- 生成迁移:   cd backend && npx prisma migrate dev --name <名称>

## 硬性规范（务必遵守）
- 任何代码改动必须带或更新测试；提交前 `cd backend && npm test` 必须通过。
- 任何数据库结构变更必须走 Prisma 迁移（禁止手改 SQL 或直接改库）。
- 迁移要向后兼容：先加列不删列，字段下线分两步。
- 只做与当前 issue 相关的最小改动，不顺手重构无关代码。
- 不要修改 .gitea/workflows/、scripts/、ecosystem.config.js 等部署相关文件（人工把关）。
- 不提交任何密钥；配置一律从 .env 读取。

## 分支 / 提交
- 分支名: feat/issue-<编号>
- 提交信息: feat: <简述> (#<编号>)
MD

git add CLAUDE.md && git commit -m "docs: add CLAUDE.md conventions" && git push
```

可选：仓库 `.claude/settings.json` 给 Claude Code 的工具再加一层限制（防误伤）：

```bash
mkdir -p .claude
cat > .claude/settings.json <<'JSON'
{
  "permissions": {
    "allow": [
      "Read", "Glob", "Grep", "Edit", "Write",
      "Bash(npm ci)", "Bash(npm test)", "Bash(npm run build)",
      "Bash(npx prisma migrate dev:*)", "Bash(npx prisma generate)",
      "Bash(git add:*)", "Bash(git status)", "Bash(git diff:*)"
    ],
    "deny": [
      "Bash(rm -rf:*)", "Bash(sudo:*)", "Bash(git push:*)", "WebFetch"
    ]
  }
}
JSON
git add .claude/settings.json && git commit -m "chore: claude code tool guardrails" && git push
```

> 注意 push 被 deny——**推分支和开 PR 由编排脚本确定性地做**，不交给 AI。真正的安全边界是：一次性 VM + ci-bot 不能合并 + CI 门 + 人工 review。settings.json 是额外防线。

---

## Part C —【VM 内】安装并认证 Claude Code

```bash
# gitea-ci VM 内（Node 20 已在阶段 0 装好；别用 sudo 装）
npm install -g @anthropic-ai/claude-code
claude --version
```

**认证**（服务器无浏览器，二选一）：

- **A. API Key（按 token 计费）**——最省事：去 console.anthropic.com 生成 `sk-ant-...`，后面写进 `.agent.env` 的 `ANTHROPIC_API_KEY`。
- **B. 订阅 token**——若你有 Claude 订阅：在**有浏览器的机器**上跑 `claude setup-token`，拿到一年期 token，写进 `.agent.env` 的 `CLAUDE_CODE_OAUTH_TOKEN`。

> 成本提醒：每次 analyze/implement 都消耗额度（A 是真金白银按量计费）。先用默认 `sonnet`，别把轮询设太密。

---

## Part D —【VM 内】建 agent 运行用户、凭据、工作区

```bash
# 专用用户，和 gitea-runner 分开
sudo useradd --create-home --shell /bin/bash coder
sudo apt install -y jq       # 脚本要用 jq 解析 API

# 切到 coder 身份操作
sudo -iu coder

# 1) git 身份 + 凭据（用 ci-bot token，非交互 push）
git config --global user.name  "ci-bot"
git config --global user.email "ci-bot@example.local"
git config --global credential.helper store
echo "http://ci-bot:<ci-bot 的 TOKEN>@gitea-ci.orb.local:3000" > ~/.git-credentials
chmod 600 ~/.git-credentials

# 2) 克隆仓库到工作区
mkdir -p ~/work && cd ~/work
git clone http://gitea-ci.orb.local:3000/<你的用户名>/rsdesign-new.git
cd rsdesign-new && npm --prefix backend ci && npm --prefix frontend ci   # 预热依赖（按实际目录结构调整）

# 3) agent 环境文件
cat > ~/.agent.env <<'ENV'
export GITEA_URL="http://gitea-ci.orb.local:3000"
export GITEA_OWNER="<你的用户名>"
export GITEA_REPO="rsdesign-new"
export GITEA_TOKEN="<ci-bot 的 TOKEN>"
# 认证二选一：
export ANTHROPIC_API_KEY="sk-ant-..."
# export CLAUDE_CODE_OAUTH_TOKEN="..."
export CLAUDE_MODEL="sonnet"
ENV
chmod 600 ~/.agent.env
```

---

## Part E —【VM 内 / coder】编排脚本

```bash
mkdir -p ~/agent
```

`~/agent/analyze.sh` —— 只读分析，贴方案评论：

```bash
cat > ~/agent/analyze.sh <<'SH'
#!/usr/bin/env bash
set -euo pipefail
source "$HOME/.agent.env"
N="$1"
API="$GITEA_URL/api/v1/repos/$GITEA_OWNER/$GITEA_REPO"
AUTH="Authorization: token $GITEA_TOKEN"
label_id(){ curl -fsS -H "$AUTH" "$API/labels" | jq -r --arg n "$1" '.[]|select(.name==$n)|.id'; }

ISSUE=$(curl -fsS -H "$AUTH" "$API/issues/$N")
TITLE=$(echo "$ISSUE" | jq -r .title)
BODY=$(echo "$ISSUE"  | jq -r .body)

cd "$HOME/work/$GITEA_REPO" && git checkout -q main && git pull -q

PROMPT="你是本项目开发助手。先读仓库结构与 CLAUDE.md。针对下面的 issue，产出简洁开发方案：受影响文件、改动点、是否涉及 Prisma 迁移、测试计划、风险点。不要改任何文件，只输出方案文本。

ISSUE #$N: $TITLE

$BODY"

PLAN=$(claude -p "$PROMPT" --permission-mode dontAsk \
  --allowedTools "Read,Glob,Grep" --model "$CLAUDE_MODEL")

COMMENT="🤖 **开发方案（待人工批准）**

$PLAN

---
批准：把标签改为 \`approved\`。需调整：在评论里说明后仍保留 \`needs-analysis\` 可重新分析。"

jq -n --arg b "$COMMENT" '{body:$b}' \
 | curl -fsS -X POST -H "$AUTH" -H "Content-Type: application/json" -d @- "$API/issues/$N/comments" >/dev/null

curl -fsS -X DELETE -H "$AUTH" "$API/issues/$N/labels/$(label_id needs-analysis)" >/dev/null || true
curl -fsS -X POST -H "$AUTH" -H "Content-Type: application/json" \
  -d "{\"labels\":[$(label_id awaiting-approval)]}" "$API/issues/$N/labels" >/dev/null
echo "已分析 #$N"
SH
chmod +x ~/agent/analyze.sh
```

`~/agent/implement.sh` —— 实现、测试、推分支、开 PR：

```bash
cat > ~/agent/implement.sh <<'SH'
#!/usr/bin/env bash
set -euo pipefail
source "$HOME/.agent.env"
N="$1"
API="$GITEA_URL/api/v1/repos/$GITEA_OWNER/$GITEA_REPO"
AUTH="Authorization: token $GITEA_TOKEN"
label_id(){ curl -fsS -H "$AUTH" "$API/labels" | jq -r --arg n "$1" '.[]|select(.name==$n)|.id'; }
fail(){ # 出错时打标签+评论，避免无限重试
  jq -n --arg b "🤖 自动开发失败：$1，转人工。" '{body:$b}' \
   | curl -fsS -X POST -H "$AUTH" -H "Content-Type: application/json" -d @- "$API/issues/$N/comments" >/dev/null || true
  curl -fsS -X DELETE -H "$AUTH" "$API/issues/$N/labels/$(label_id approved)" >/dev/null || true
  exit 1; }

ISSUE=$(curl -fsS -H "$AUTH" "$API/issues/$N")
TITLE=$(echo "$ISSUE" | jq -r .title)
BODY=$(echo "$ISSUE"  | jq -r .body)
PLAN=$(curl -fsS -H "$AUTH" "$API/issues/$N/comments" \
       | jq -r '[.[]|select(.body|startswith("🤖 **开发方案"))]|last|.body')

cd "$HOME/work/$GITEA_REPO"
git checkout -q main && git pull -q
BR="feat/issue-$N"
git checkout -qB "$BR"

PROMPT="按下面已批准的方案实现 issue #$N。严格遵循仓库根 CLAUDE.md：改动带/更新测试；涉及 schema 用 prisma migrate dev 生成迁移；完成后运行 cd backend && npm test 确保通过。只做与本 issue 相关的最小改动。

已批准方案:
$PLAN

ISSUE: $TITLE
$BODY"

claude -p "$PROMPT" --permission-mode bypassPermissions --model "$CLAUDE_MODEL" || fail "Claude Code 执行报错"

# 硬门：脚本再独立跑一遍测试
( cd backend && npm test ) || fail "测试未通过"

git add -A
git commit -q -m "feat: implement #$N — $TITLE" || fail "无代码改动"
git push -q -u origin "$BR" || fail "push 失败"

jq -n --arg t "feat: #$N $TITLE" --arg b "自动实现 issue #$N。

Closes #$N" --arg h "$BR" \
  '{title:$t, body:$b, head:$h, base:"main"}' \
 | curl -fsS -X POST -H "$AUTH" -H "Content-Type: application/json" -d @- "$API/pulls" >/dev/null || fail "开 PR 失败"

curl -fsS -X DELETE -H "$AUTH" "$API/issues/$N/labels/$(label_id approved)" >/dev/null || true
curl -fsS -X POST -H "$AUTH" -H "Content-Type: application/json" \
  -d "{\"labels\":[$(label_id pr-open)]}" "$API/issues/$N/labels" >/dev/null
echo "已为 #$N 开 PR（分支 $BR）"
SH
chmod +x ~/agent/implement.sh
```

`~/agent/poll.sh` —— 扫两种标签：

```bash
cat > ~/agent/poll.sh <<'SH'
#!/usr/bin/env bash
set -euo pipefail
source "$HOME/.agent.env"
API="$GITEA_URL/api/v1/repos/$GITEA_OWNER/$GITEA_REPO"
AUTH="Authorization: token $GITEA_TOKEN"
list(){ curl -fsS -H "$AUTH" "$API/issues?type=issues&state=open&labels=$1" | jq -r '.[].number'; }

for N in $(list needs-analysis); do "$HOME/agent/analyze.sh"   "$N" || echo "分析 #$N 失败"; done
for N in $(list approved);       do "$HOME/agent/implement.sh" "$N" || echo "实现 #$N 失败"; done
SH
chmod +x ~/agent/poll.sh
```

**先手动测一次**（建议先建个测试 issue 打上 `needs-analysis`）：

```bash
source ~/.agent.env
~/agent/poll.sh
```

---

## Part F —【VM 内】systemd 定时轮询

以 `coder` 用户身份装用户级 timer（每 15 分钟）：

```bash
# 仍在 coder 身份下
mkdir -p ~/.config/systemd/user

cat > ~/.config/systemd/user/agent-poll.service <<'UNIT'
[Unit]
Description=AI agent poll Gitea
[Service]
Type=oneshot
ExecStart=%h/agent/poll.sh
UNIT

cat > ~/.config/systemd/user/agent-poll.timer <<'UNIT'
[Unit]
Description=Run agent poll every 15 min
[Timer]
OnBootSec=2min
OnUnitActiveSec=15min
[Install]
WantedBy=timers.target
UNIT

systemctl --user daemon-reload
systemctl --user enable --now agent-poll.timer
systemctl --user list-timers | grep agent-poll
```

```bash
# 让 coder 的 user 服务在未登录时也运行
# 需回到有 sudo 的账号执行一次：
exit    # 退出 coder
sudo loginctl enable-linger coder
```

看日志：`journalctl --user -u agent-poll.service`（在 coder 身份下）或 `sudo journalctl _UID=$(id -u coder)`。

---

## Part G — 端到端验证（把整条一条龙跑一遍）

1. **【手动】** 在 `myapp` 提一个真实小需求 issue，例如标题「/api/notes 返回时增加 count 字段」，正文简述期望。给它打标签 **`needs-analysis`**。
2. 等下一轮 poller（或手动 `~/agent/poll.sh`）。issue 下应出现 🤖 **开发方案** 评论，标签变 `awaiting-approval`。
3. **【人工闸门1】** 你看方案，OK 就把标签改成 **`approved`**。
4. 下一轮 poller → 自动建分支、改代码、`npm test`、push、开 PR，标签变 `pr-open`。
5. PR 上 **CI（ci.yml）** 自动跑绿。
6. **【人工闸门2】** 你 review PR → **Merge**。
7. 合并触发 **deploy-test.yml** → 测试环境更新（`http://gitea-ci.orb.local:8090`）。
8. **【人工】** 验证测试环境 → 用阶段 2 的 `promote-to-prod.sh` 上 `prod-sim`。

跑通这一遍，你的「issue → AI → 人审 → 自动开发 → CI → 部署」完整闭环就成立了。

---

## ✅ 阶段 2.5 验收清单

- [ ] `ci-bot` 账号 + 最小权限 token（write:issue / write:repository）
- [ ] main 分支保护开启，ci-bot 无法直接合并
- [ ] 4 个流程标签建好
- [ ] 仓库根 `CLAUDE.md`（+ 可选 `.claude/settings.json`）已提交
- [ ] VM 内 Claude Code 装好并认证（`claude --version` OK）
- [ ] `coder` 用户、git 凭据、`~/work/rsdesign-new` 克隆、`~/.agent.env` 就位
- [ ] `analyze.sh` / `implement.sh` / `poll.sh` 手动跑通
- [ ] systemd user timer 每 15 分钟运行，`enable-linger` 已开
- [ ] 端到端：一个 issue 走完「分析→批准→PR→CI→合并→部署」

---

## 安全与成本提醒

- **权限边界**：ci-bot 只能推分支/开 PR，不能合并（分支保护）；Claude Code 的自主性被"一次性 VM + 不能合并的 token + CI 门 + 人工 merge"四重兜底。
- **别把 token 写进仓库**：`.agent.env`、`.git-credentials` 都在 coder 家目录、`chmod 600`，不进 git。
- **成本**：API Key 模式按量计费；轮询间隔别太短，模型先用 `sonnet`。有订阅可用 `CLAUDE_CODE_OAUTH_TOKEN` 走订阅额度。
- **失败不空转**：`implement.sh` 失败会撤下 `approved` 标签并留言转人工，不会反复重试。
- **平移到内网/生产**：真机上跑自主 agent 前，建议把 coder 用户进一步收紧（独立机器或容器、只读挂载无关目录），并确认 `--permission-mode` 的当前行为（用 `claude --help` 核对）。

---

## 常见坑速查

| 现象 | 排查 |
|------|------|
| `claude -p` 卡住不动 | 认证没配（`ANTHROPIC_API_KEY`/`CLAUDE_CODE_OAUTH_TOKEN`）；或缺 `--permission-mode` 导致等待交互确认 |
| push 要求输入密码 | `~/.git-credentials` 格式/权限；token 是否含 `write:repository` |
| 开 PR 返回 403 | ci-bot 是否是仓库 Write 协作者；token scope |
| 标签换不动 | `label_id` 取不到——确认 4 个标签名字完全一致（含大小写）|
| poller 不触发 | `systemctl --user list-timers`；未登录不跑→`loginctl enable-linger coder` |
| 分析/实现读不到方案 | 评论匹配前缀 `🤖 **开发方案`——保持 analyze 里的评论开头不变 |
| 每轮重复处理同一 issue | 确认脚本末尾的换标签成功了；标签没换会被下一轮再捞到 |

---

*token、用户名、仓库名请按实际替换。任一步卡住，把命令和报错贴给我，我帮你定位。整条链路里我随时能帮你分析某个具体 issue、或改进 CLAUDE.md 规范。*
