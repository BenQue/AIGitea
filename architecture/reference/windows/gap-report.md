# Windows reference dry-run

This synthetic reference proves schema/profile/lock compatibility only. It has no mapped formal
application or server and did not inventory, install or mutate Windows Server, IIS, .NET or PostgreSQL.
The lock is **DRY-RUN CANDIDATE / NOT DEPLOYED**. Real adoption requires a project Change, exact host
inventory, hosting-bundle evidence, database backup/restore and IIS rollback validation.
