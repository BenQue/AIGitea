# 06 · 运维手册与踩坑集

> 排障先查本册。踩坑集全部来自 2026-07-10/11 试点**实证**（不是理论风险），正文各册引用的 🕳️ 编号以此处为准。

## 1. 日常命令速查

```bash
# ── 看部署 ────────────────────────────────────────────
orb -m gitea-ci sudo -u gitea-runner pm2 list                 # rsdesign-new 应 online
orb -m gitea-ci curl -fsS http://127.0.0.1:3100/api/health    # {"status":"ok"}
orb -m gitea-ci readlink /opt/rsdesign-test/current           # 当前 release 的 sha
orb -m gitea-ci ls -lt /opt/artifacts | head                  # 制品清单

# ── 看日志 ────────────────────────────────────────────
orb -m gitea-ci sudo tail -50 /opt/act-runner/.pm2/logs/rsdesign-new-error-2.log
orb -m gitea-ci bash -c 'sudo zstd -dc /var/lib/gitea/data/actions_log/admin/rsdesign-new/<xx>/<runid>.log.zst | tail -50'

# ── agent ────────────────────────────────────────────
orb -m gitea-ci sudo -u coder bash -c 'source ~/.agent.env && ~/agent/poll.sh'   # 手动触发轮询

# ── 回滚到指定版本 ─────────────────────────────────────
orb -m gitea-ci sudo -u gitea-runner bash -c '
  set -a; source /opt/rsdesign-test/.env; set +a
  ln -sfn /opt/rsdesign-test/releases/<旧sha> /opt/rsdesign-test/current
  pm2 delete rsdesign-new; pm2 start /opt/rsdesign-test/current/ecosystem.config.cjs --env production; pm2 save'

# ── SQLite 备份(WAL 下禁止直接 cp) ────────────────────
orb -m gitea-ci sudo -u gitea-runner sqlite3 /opt/rsdesign-test/shared/db/app.db \
  ".backup /opt/rsdesign-test/shared/storage/backups/app-$(date +%Y%m%d).db"

# ── 种子/重置测试数据(先停应用,避免锁) ─────────────────
# 流程:pm2 delete → (可选 rm shared/db/app.db*) → migrate deploy → npx tsx scripts/seed-fixtures.ts → pm2 start
```

## 2. 踩坑集（🕳️ 编号即正文引用号）

| # | 现象 | 根因 | 对策（已固化处） |
|---|------|------|------------------|
| 1 | `next build` 挂死 20+ 分钟无输出 | `next/font/google` 构建期拉 Google Fonts，弱网 TLS 黑洞 | 字体一律 `@fontsource-variable/*` 自托管（已改） |
| 2 | 构建被杀却「成功」，制品里 `.next` 是空的 | **Next 收到 SIGTERM 以 exit 0 退出**，`bash -e` 误判 | `pack.sh` 校验 `.next/BUILD_ID`（已加） |
| 3 | 部署了新制品，跑的还是旧代码/直接 crash | **PM2 reload 不应用新配置的 cwd/script**，钉死旧 release 实路径 | 部署一律 `pm2 delete + start`（已改） |
| 4 | 部署「健康检查通过」但 pm2 显示 errored | 同端口**野生进程**替真身应答了健康检查 | 部署后断言 `pm2 jlist` 目标进程 online（已加）；别在 VM 手动 `npm start` 长跑 |
| 5 | `orb ... pkill -f "xxx"` 返回 255 且无输出 | pattern 匹配到**自己所在的 bash -c 命令行**，自杀 | 模式写 `"[x]xx"` 形式，或先 pgrep 确认 |
| 6 | pm2 重启后应用丢环境变量 | `--update-env` 用当前 shell 环境**整体替换** | 重启前必 `set -a; source /opt/*/.env; set +a` |
| 7 | 用 admin token 建 token 返回 401 | Gitea **token 管理端点只认 basic auth** | `curl -u admin:密码 -H "Sudo: 目标用户" POST .../users/<u>/tokens` |
| 8 | 某用户所有 API 403："You must change your password" | **管理员创建的用户默认 must_change_password=true** | `PATCH /api/v1/admin/users/<u>` 带 `{login_name,source_id,must_change_password:false}` |
| 9 | API 返回纯文本 "400 Bad Request" | 凭据文件里有**失败重试留下的同名脏行**，`grep\|awk` 拼出多行畸形 header | 追加凭据前先清同名行；解析后校验长度 |
| 10 | 部署迁移步骤 `database is locked` | `prisma migrate deploy` 要 SQLite **独占锁**，运行中应用持连接 | 部署顺序：**停应用 → 迁移 → 切链 → 启动**（已改）；deploy-prod.sh 同序 |
| 11 | 纯文档合并也跑了完整构建部署 | deploy-test.yml 无 paths 过滤 | 无害（顺带回归）；嫌慢可加 `paths-ignore: ["docs/**"]` |
| 12 | `sudo chmod /home/coder/agent/*.sh` 报文件不存在（文件明明在） | **通配符在调用方 shell 展开**，无权限读目标目录时静默传字面量 | glob 放进 `sudo bash -c '...'` 里执行 |
| 13 | Prisma 引擎/依赖下载慢或断 | 境外源直连 | `.npmrc` 走 Verdaccio；`PRISMA_ENGINES_MIRROR=https://registry.npmmirror.com/-/binary/prisma`（CI 已设） |
| 14 | 制品在别的机器起不来（引擎不匹配） | Prisma query engine 平台相关 | schema 已配 `binaryTargets = ["native","linux-arm64-openssl-3.0.x","debian-openssl-3.0.x"]`；新平台先 `openssl version` 对号 |

## 3. 凭据清单（位置备忘，无明文）

| 凭据 | 存放 | 用途 |
|------|------|------|
| Gitea admin 密码/token | gitea-ci VM `~benque/gitea-ci-credentials.txt` | 管理操作 |
| ci-bot PAT（`agent-20260710`） | 同上 + coder `~/.agent.env` + coder `~/.git-credentials` | agent API/git |
| Claude 订阅 OAuth token（一年期） | coder `~/.agent.env` | headless claude |
| 部署 SSH 私钥（→prod-sim） | `/opt/act-runner/.ssh/deploy_key`（gitea-runner） | promote |
| 测试环境应用配置 | `/opt/rsdesign-test/.env`（gitea-runner，600） | 部署注入 |

轮换要点：换 ci-bot PAT 要同步 `.agent.env` 与 `.git-credentials` 两处；换 Claude token 只动 `.agent.env`。

## 4. 排障决策树（部署失败时）

1. 看 Actions 日志定位失败步骤（§1 命令）
2. 构建步失败 → 🕳️ 1/2/13；打包步 → 🕳️ 2；部署步 → 🕳️ 10/3/6；健康检查步 → 🕳️ 4（查 pm2 error log）
3. pm2 errored → `sudo tail /opt/act-runner/.pm2/logs/rsdesign-new-error-2.log`
4. 修复走 PR（分支保护对所有人生效）；修完 revert 或直接下一次合并会重新部署
