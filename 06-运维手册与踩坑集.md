# 06 · 运维手册与踩坑集

> 排障先查本册。踩坑集全部来自 2026-07-10/11 试点**实证**（不是理论风险），正文各册引用的 🕳️ 编号以此处为准。

## 1. 日常命令速查

```bash
# ── 正常 Gitea/host 只读入口 ───────────────────────────
/usr/local/libexec/aisoft/host-access-broker \
  --project aisoft-platform --operation gitea.repo.read
/usr/local/libexec/aisoft/host-access-broker \
  --project aisoft-platform --operation host.access.audit
/usr/local/libexec/aisoft/host-access-broker \
  --project aisoft-platform --operation orbstack.vm.status

# ── host-role guard（只在已配置 live profile 的 host context）
orb -m gitea-ci -u gitea-runner /usr/local/libexec/aisoft/verify-host-role \
  --action run --resource build                              # 预期 0
orb -m gitea-ci -u gitea-runner /usr/local/libexec/aisoft/verify-host-role \
  --action start --resource application                      # scm-ci 预期 20
```

live profile 固定为 `root:gitea-runner 640`；因此手工探针也固定使用已授权的 `gitea-runner`
身份。安装包含 #97 的 exact-main guard 后，普通 VM 用户照抄 guard 命令会因无 profile read 权限
返回 rc=30 `profile-permission-denied`；安装前的 guard 可能只返回笼统 `invalid-json`。二者都表示调用
身份不符合合同，不是 profile/schema 迁移信号；不得通过放宽 mode、改变 group membership 或复制
profile 来绕过。

历史 `/opt/rsdesign-test`、`gitea-ci:3100/8091`、`sfm-board:3212`、MyApp `8090`、Redis 和
`prod-sim` 已在 Issue #21 收口，不再提供日常查询、日志、回滚或删除命令。需要追溯时只读对应
verification；任何恢复或重建都必须新开 Issue，不能把历史命令当作当前入口。

受限 sandbox 中 `orb` 可能返回 `timed out waiting for VM to start`，这只能说明执行路径被阻断，
不能证明 VM 停机。Issue #61 发布前使用已经批准的 host execution path 重复同一只读 probe；
发布并安装后正常入口改为下述 broker。不得为排除该错误而 start/restart machine。

### 1.0 Host access broker 正常路径

Issue #61 提供 strict `host-access-broker/v1`，Issue #67 修复 Git credential protocol multi-valued
attributes，Issue #70 补齐 governed Issue/PR writes 与独立 worktree push。各 Change 合并后都只能从 exact
protected-main SHA 安装；正常 Gitea/Git/OrbStack 访问必须从固定入口开始：

```bash
/usr/local/libexec/aisoft/host-access-broker \
  --project aisoft-platform \
  --operation gitea.issue.read \
  --number 61

/usr/local/libexec/aisoft/host-access-broker \
  --project newemaint \
  --operation orbstack.vm.status
```

调用者只能选择 manifest project、allowlisted operation 及其 typed argument；不能传 URL、owner/repo、
checkout/credential path、shell、refspec、force 或 merge。project API/Git 路由自己的 project-agent，
protection/settings read 路由 manager audit，repository mutation credential 与 ordinary Git 分离，
人工 `admin` 不进入 broker credential catalog。Git push 只允许当前 checkout 的同名 `change/N-short-description`；legacy `change/N` 只有远端同名 ref 已存在时才允许维护。

Codex 的持久 host execution authorization 只能绑定 exact
`/usr/local/libexec/aisoft/host-access-broker` prefix；不得持久授权 generic `curl`、broad `git push`、
credential-store command 或 source-tree wrapper。只有 fresh task 中重复执行 typed Issue mutation/readback、
exact readable change-ref push、PR create/read 与 protection/status readback 均无提示，才可把端到端结果记为 `PASS`。

Issue #70 supersede Issue #61 的 Mac Keychain binding：Mac credential root 固定为 repo-external
`/Users/benque/Library/Application Support/AISoftPlatform/credentials`。manager audit/mutation 分文件，
project-agent 按 manifest `project_id` 分目录；调用方不能传 path。所有 managed 目录必须 owner `benque` +
mode `700`，token 必须 owner `benque` + mode `600` + regular non-symlink + single hardlink + strict 单行。
broker failure 不输出 path 或 bytes；installer 不创建、复制或更新 token。repo binding 只写 local
helper/username/`credential.useHttpPath`，不写 token/path。VM profile 继续使用 `GITEA_IDENTITY` + fixed
`GITEA_TOKEN_FILE`；inline `GITEA_TOKEN` 不再是 profile contract。

Issue #73 的 protected-main source 为每个 project 增加 optional strict `git_remote_name`；未声明时解析为 `origin`，
NewEmaint 精确解析为 `gitea`，其 GitHub `origin` 不得创建、删除、重命名或改写。`git.fetch.main`、
`git.fetch.change`、`git.push.change` 与 `mac.git.bind` 只使用 manifest remote，并在 credential resolution
前要求该 remote 的 fetch/push URL 都唯一且精确等于 governance 派生 Gitea URL。调用方不能传 remote
name、URL、owner/repository、refspec 或 command。

Issue #136 解开 `git.push.change` 的三条约束互锁。此前的推送路径同时要求：基线新鲜
（`git merge-base --is-ancestor origin/main HEAD`，否则 `BASE_BRANCH_STALE`）、线性历史
（`origin/main..HEAD` 无 merge commit，否则 `MERGE_COMMIT_DENIED`）、以及裸
`git push remote refs/heads/X:refs/heads/X`。`main` 一前进三者即互锁：第一条要求 rebase，
rebase 重写 commit 对象使裸 push 变成 non-fast-forward 而执行不了，改用 merge 引入 `main`
又被第二条拒绝。任何**已推送**的 change 分支因此永久无法再推，唯一出路是人工到 Gitea UI
删除远端分支——broker 的 typed 操作清单里没有 branch delete，而绕过 broker 直接
`git push --delete` 是治理禁止的。触发条件是「分支已推送 + `main` 前进 + 分支还需再推」：
单 Issue 串行推进时几乎碰不到，**并行 Issue 一多就成为常态**，任一 PR 合并都会让其余在途
分支进入该状态。现象还具误导性——调用方看到的是 `HOST_COMMAND_FAILED: structured host
operation failed`，既不提 non-fast-forward，也不提 rebase。

修复后 `git.push.change` 使用 `--force-with-lease=refs/heads/<branch>:<remote-sha>`。
`<remote-sha>` 取自同一次操作里推送前已经执行的 `git ls-remote --heads`（该结果本来就用于
change name 唯一性判定，只是此前丢掉了 sha）。远端分支尚不存在时使用**空 expectation**
（`refs/heads/<branch>:`），git 的语义是「该 ref 必须仍不存在」：首推正常创建，而若在读取与
推送之间被别人抢先创建则拒绝——这比「不存在就省略 lease」更强，后者在首推竞态下会退化成
无保护的裸 push。force 的作用面由既有的两层夹住：`change_branch()` 已把可推送 refspec 限定为
`change/*`，`main` 的 branch protection 有 `enable_force_push: false` 并由 `host.access.audit`
持续核对，force 够不到 `main`。同时 `ls-remote` 返回的 object id 现在必须是 40 位小写 hex，
否则 `RESPONSE_SCHEMA_INVALID`。

新增错误码 `REMOTE_BRANCH_MOVED` 取代此处无信息量的 `HOST_COMMAND_FAILED`：推送输出命中
`stale info`、`non-fast-forward` 或 `fetch first` 时返回，含义是**远端 change 分支在 broker
读取 lease 之后被改动，推送已被拒绝而非覆盖**。处置是重新 `git.fetch.change` 看清远端变化后
再判断（多半是同一 Issue 出现第二个写者，或远端分支被人工改过），而不是提高 force 力度绕过去。
lease 命中时 force 必定成功，所以这个码不会与凭据、网络故障混淆；其余推送失败仍归
`HOST_COMMAND_FAILED`。`BASE_BRANCH_STALE` 与 `MERGE_COMMIT_DENIED` 两条检查保持不变——
互锁解开的是「rebase 之后推不上去」，不是放宽基线新鲜与线性历史要求。

生效边界照旧：**source 合并不改变 installed broker 的字节**。Mac 与 gitea-ci VM 的
`/usr/local/libexec/aisoft/host-access-broker`（runtime 在 `/usr/local/lib/aisoft-host-access/`）
各自重装前，互锁在实机上依然存在，包括 #136 自己的分支。这是 broker 行为变更的通例：
新增或修改 typed 操作后必须两台重装并回读 installed 字节，只读 source diff 不构成生效证据。

Issue #143 补上 CI 证据的两个只读操作。此前 CI 证据只能停在 `gitea.commit.status.read`，
它给出 `context`/`status`/`description`/`target_url` 四项，其中 `target_url` 是一个 Web UI 路径，
broker 没有任何操作能顺着它取到内容。后果是双向的：绿了读不懂（无法核实某个步骤到底执行了没有），
红了更读不懂（一次失败只有 `status = failure`，没有失败步骤名、没有报错输出，而 runner 是 host
模式、走本机 Verdaccio 与 PGDG PostgreSQL 17，本地环境与它并不等价）。

```bash
# 由 commit SHA 拿 job/step 级结论（步骤名、状态、结论、耗时），不含日志正文
/usr/local/libexec/aisoft/host-access-broker \
  --project localwms --operation gitea.actions.run.read --sha <40 位小写 SHA>

# 由上一步返回的 job id 拿该 job 的日志（尾部截断 + 脱敏）
/usr/local/libexec/aisoft/host-access-broker \
  --project localwms --operation gitea.actions.job.logs.read --job <id>
```

四条要点：

- `--sha` 必须**精确 40 位小写**。缩写 SHA 走 `ARGUMENT_INVALID`，在发出请求之前就被拒绝——
  与 `gitea.commit.status.read` 在缩写 SHA 上返回 `statuses: []` 的假成功正相反。合法 SHA 但
  没有运行则返回 `{"total_count": 0, "runs": []}`，与读取失败（`HTTP_404` 等）不同形。
- job id 来自 `run.read` 的返回，**不需要从 `target_url` 里抠**。
- 日志上限 64 KiB，取**尾部**（失败步骤的报错打印在最后），截断时 `truncated`/`original_bytes`/
  `returned_bytes` 显式给出，正文前置一行标记；脱敏次数由 `redactions` 报出。脱敏是两层：
  Gitea 自己掩注册过的 Actions secret，broker 再掩本次凭据明文、`Authorization` 值、URL userinfo、
  含 TOKEN/SECRET/PASSWORD 等字样的赋值、GitHub 式前缀 token 与 PEM 私钥块。**裸 40-hex 不掩**——
  它与 commit SHA 同形，掩掉会让日志失去最有用的字段。
- 只读：没有 rerun/cancel/dispatch 的 typed 对应物，触发或重跑 CI 仍是人的动作，并有测试钉住。

新项目或已有项目 adoption 必须逐项目执行以下闸门，不得批量启用：先运行只读
`host.access.audit`；credential 缺失时停止并取得独立 Secret mutation approval；随后运行
`mac.git.bind`，最后以只读 `host.onboarding.check` 同时回读 protected-file metadata、token identity/scope、
repository permission、protected `main`/required CI、canonical checkout、remote fetch/push URL 与 exact
repo-local helper binding。任一缺失或 drift 均 fail closed；Issue/平台合同 approval 不等于 credential
provision approval。source 合并不替代 installed-byte readback；NewEmaint 与其它项目 adoption 仍分别以
自己的 Issue、binding、typed canary、CI 和 no-op evidence 为准。

fixed Git helper 按 Git credential protocol 分开处理 scalar 与 multi-valued attributes：`protocol`、
`host`、`path`、`username` 必须在 strict allowlist 内且各自唯一；`capability[]`、`wwwauth[]` 和其它
不影响 basic username/password route 的合法 `key[]` 可以重复、保持输入顺序并在解析后忽略。unknown
scalar、malformed line、identity/host/path/cross-project mismatch 继续 fail closed。协议诊断只记录字段名，
不得记录 `key[]` values、challenge、username/password、credential path 或 token bytes。

`orbstack-access-diagnostics` 仅是 emergency fallback：只有 broker 自身失败、脱敏错误无法区分且
host/sandbox 真实状态仍矛盾时才能运行。正常 read/push/profile/timer 验收调用次数必须为 0；不能把
诊断技能当成每次任务的前置步骤。Issue #61 verification 中的 pre-merge `NOT RUN` 只保留为当时
证据；当前是否可用必须运行本节 installed broker 的 typed readback，不能从旧 Change 状态推定。

#### 安装面归属（哪个安装器在哪台机器执行）

安装器按**安装根**而非按主机命名，容易被误读成「只装一台」。实际归属如下：

| 安装器 | 安装根 | 需 root | Mac host | VM `gitea-ci` | 提供 |
|---|---|---|---|---|---|
| `codex/install-vm.sh` | `$TARGET_HOME`（如 `/home/coder`） | 否 | 否 | **是** | Loop runtime、Codex skills、agent 脚本、`gitea-readonly.sh`、user-scope manifests、disabled systemd 模板 |
| `codex/install-host-role.sh` | `/usr/local` + `/etc/aisoft` | 是 | 是 | **是** | `verify-host-role` guard、host-role schema/catalog |
| `codex/install-host-access-broker.sh` | `/usr/local` | 是 | **是** | **是** | `host-access-broker`、`git-credential-aisoft-host`、`project-profile-migration`、`aisoft_host_access` / `aisoft_gitea_governance` runtime、`/usr/local/share/aisoft/` 两个 manifest |

关键点：`install-host-access-broker.sh` **两台都要装**。Mac 侧提供 broker 入口本身；VM 侧提供
`project-profile-migration` 及其依赖，因为 `vm.profile.plan/apply/read-back/rollback` 是由 Mac 上的
broker 通过 `orb -m gitea-ci -u <user> sudo -n <vm_profile_tool>` 在 **VM 内**执行的。只装 Mac 会让
四个 `vm.profile.*` 操作全部不可用（踩坑 18）。

`install-vm.sh` 是 user-scoped 且不需要 root，**不能**替代上面两个 root-scoped 安装器；它写入
`$TARGET_HOME/.local/share/aisoft/`，与 `project-profile-migration` 期望的
`/usr/local/share/aisoft/` 不是同一路径。

三个安装器都幂等且不绑定凭据。VM 侧安装面自检：

```bash
orb -m gitea-ci -u benque bash -lc '
  ls /usr/local/libexec/aisoft/ /usr/local/lib/aisoft-host-access /usr/local/share/aisoft/'
```

`project-profile-migration`、非空的 `aisoft-host-access` 包目录、以及
`host-access-broker.json` + `gitea-governance.json` 三者必须同时在位。

### 1.1 私有 Gitea 的只读检查

先从目标 checkout 的 Gitea remote 或显式 project profile 核对 `GITEA_URL`、`GITEA_OWNER`、`GITEA_REPO`。**不要用匿名 API 判断私有仓库是否存在**：匿名 `404` 和 Git `Repository not found` 可能只是当前身份没有可见性。

按以下顺序访问：

1. 优先使用 §1.0 broker（#61）；project/operation/identity/target 必须来自 exact manifests。
2. VM 内部 poll 使用 mode 600 exact profile + fixed token file；legacy read-only helper 只作受控兼容。
3. refs/SHA/ancestry 使用 broker 的 Git operation 或已完成 repo-local binding 的 fixed helper。
4. broker 未发布/不可用时，才使用已批准 host execution path 或 VM-local 管理员只读 helper；不复制 token、不扩 ACL。
5. 必须观察 UI 时，使用现有已登录浏览器会话做只读回退。

Exact profile 示例：

```bash
orb -m gitea-ci -u coder bash -lc '
  AGENT_ENV_FILE=/home/coder/.config/aisoft/projects/<profile>.env \
  GITEA_EXPECT_OWNER=<owner> \
  GITEA_EXPECT_REPO=<repo> \
  /home/coder/agent/gitea-readonly.sh "issues?state=all&type=issues&limit=50"
'
```

VM-local 管理员只读 fallback（helper 未安装时使用 MyDocs 权威 source）：

```bash
orb -m gitea-ci -u benque bash -lc '
  GITEA_URL=http://127.0.0.1:3000 \
  GITEA_OWNER=<owner> \
  GITEA_REPO=<repo> \
  GITEA_EXPECT_OWNER=<owner> \
  GITEA_EXPECT_REPO=<repo> \
  GITEA_CREDENTIAL_FILE=/home/benque/gitea-ci-credentials.txt \
  /mnt/mac/Users/benque/MyDocs/AISoftPlatform/codex/tools/gitea-readonly.sh \
    "issues?state=all&type=issues&limit=50"
'
```

helper 只允许 GET，并通过 curl stdin config 传 token。调用方再用窄 `jq` 投影输出必要字段；不得打印 profile、credential file、request header 或 curl config。Issue 评论、标签、PR、仓库设置、权限、merge/close 等 mutation 仍走专用脚本或明确授权的浏览器操作。

### 1.2 Gitea governance manifest 与 project-agent gate

唯一目标合同（#35，已 live）是
[`codex/config/gitea-governance.json`](codex/config/gitea-governance.json)：

- 默认 private；public 只能来自 exact allowlist，不能按仓库名、当前状态或搜索结果推导。
- `aisoft-platform-manager` 不是 site admin，但在 manifest 明确列出的仓库为 Admin；audit 与
  mutation PAT 分离，不进入 project profile、普通 Git 或 merge allowlist。
- 每个 repository 只有一个 manifest-declared project agent，权限精确为 Write；另一个项目的
  agent 出现 Write/Admin 即 `BLOCKED_EXTERNAL`。
- `main` 禁止 direct/force push，merge allowlist 只能是人工 `admin`，并保留 manifest 中的 exact
  required status contexts/approvals。缺失 protection 时创建最小保护，不凭空添加 CI context。
- `gitea-governance.sh check` 默认完全只读；`bootstrap-manager`、`apply`、`rollback` 和
  `retire-shared-bot` 一次只能指定一个 exact repository，mutation 还必须证明 Issue #35 manifest
  byte-identical 地存在于已合并 `origin/main` SHA。
- 未知仓库只 report；新增项目或 public 例外必须先通过 AISoftPlatform Issue/PR 修改 manifest。

required status context 的普通 drift 仍然 fail closed。只有 exact repository 在 manifest 中声明
`required_context_migration`，且该声明把唯一 canonical context 绑定到真实成功的 PR/head SHA、
`pull_request` event 与 `success` state 时，操作员才可显式选择窄迁移模式：

```bash
bash codex/tools/gitea-governance.sh \
  --manifest codex/config/gitea-governance.json \
  check --token-file <manager-audit-token-file> \
  --repository <manifest-repository> \
  --required-context-migration
```

read-only plan 只有在 live 起点为 status check disabled + empty contexts，且其它 repository、
permission 与 protection 字段全部无 drift 时，才显示 `migrate-required-status-context` 且无 blocker。
后续 `apply` 仍须满足 Issue #35 merged-manifest gate、独立 live mutation 授权、新的 mode 700 evidence
目录，并显式传同一个布尔 selector；CLI 不接受 URL、owner、context、force、bypass 或 legacy 参数。

apply 在任何 PATCH 前以 mode 600 原子持久化 `<repository>-pre.json`，PATCH 后完整读回并写
`<repository>-post.json`。PATCH 或读回失败时不得复用 evidence 目录；保留 pre snapshot，并在独立
rollback 授权下对同一 manifest repository 使用既有 `rollback --snapshot <pre.json>`，rollback 会在
mutation 前验证 snapshot schema/repository/main branch，恢复后再次完整读回。不得手工编辑 snapshot，
也不得把一个 repository 的 snapshot 用于另一个 repository。

账号创建使用 `bootstrap-gitea-service-account.sh`，一次只接受一个 declared identity/token kind。
Gitea CLI raw token 只写 mode 600 managed credential root；账号已存在但没有 Issue #35
ownership marker、token marker 存在但 credential 丢失、scope/identity/site-admin read-back 不符均
fail closed。bot user 只使用 `--user-type bot`；Gitea 1.26.4 的 create 命令不接受 `--password`、
`--random-password` 或 `--must-change-password`，但 live build 仍可能把新 bot 置为必须改密。bootstrap
必须随后用专用 `admin user must-change-password --unset <exact-username>` 清除该状态，不设置密码，并写
mode 600 Issue #35 policy marker；marker 缺失时可恢复一次，存在后必须内容/权限精确匹配且幂等运行不再
重复数据库 mutation。然后才由独立命令生成 manifest-declared 最小 scope PAT。
受保护 `GITEA_CONFIG` 的存在性与可读性必须通过 `sudo -n -u git test -f/-r` 以实际 Gitea OS user
预检；调用 bootstrap 的普通运维用户不需要、也不得因此获得 config 读取权限。Gitea binary 的可执行性
仍由 caller 预检，credential root、临时目录、stdout redirection 和最终 mode 600 token 文件继续只归
caller 管理，不能用 root 运行整个 bootstrap 来绕过边界。
不得把 manager 或 project PAT 复制到 Mac 通用 credential、其它 profile 或聊天记录。

`sync-gitea-service-policy.sh --check` 只读核对 `DISABLE_REGISTRATION=true`、
`DEFAULT_PRIVATE=private`、`FORCE_PRIVATE=false`、`REQUIRE_SIGNIN_VIEW=false`。`--apply` 先生成
root-only 原始 config backup；mode 600 candidate 只交给显式且存在的 Gitea service OS user（本地
默认 `git`），并通过 `sudo -n -u` 运行 doctor。config install、restart、health 和 rollback 继续由
root 管理；doctor 通过后才 restart/health，失败自动恢复原始 bytes。不得以 root 直接启动 Gitea
binary 做 doctor，也不得把 Gitea PAT 与该 OS identity 混用。restart readiness 默认最多 30 次、
每次间隔 1 秒；每次同时要求 systemd active 和 healthz 成功。attempts 只允许 1–60，interval 只
允许 0 或 1 秒；预算耗尽才进入原始 config restore，恢复后的 restart 也使用同一有界检查。

#### 旧 `ci-bot` gate（已退役）

Issue #35 live reconciliation 已完成：共享 `ci-bot` 账号保留，但已从全部 manifest 仓库移除
collaborator 权限。`ensure-gitea-collaborator.sh` 与旧 profile 命令只保留为历史兼容/回归测试，
不得在当前运维中运行、回加 Write、用于新项目或作为 broker fallback。

当前访问失败时应返回 `BLOCKED_EXTERNAL`，核对 exact project agent、protected credential、manifest、
repository permission、protected `main` 和 repo-local binding；不得通过恢复共享 bot 或扩大 manager
权限绕过。历史迁移步骤和回滚证据见 `docs/changes/35/03-verification.md`。

### 1.3 Host-role guard 与 destructive Gate

`codex/install-host-role.sh` 只安装 schema、capability catalog、guard 和 profile 示例；它不
创建 `/etc/aisoft/host-profile.json`，不写凭据，不 enable/start 服务。live profile 必须在
目标主机自己的 complex Change 中从示例生成，填入真实 `hostname` 与 `/etc/machine-id`，
保持 catalog 的精确 role capability 集，并设置 root ownership 与 `400/440/600/640` mode。

应用仓库的 deploy/start/database wrapper 必须调用固定路径
`/usr/local/libexec/aisoft/verify-host-role`。调用方只传 catalog 中的固定 `--action` 与
`--resource`，不能传 profile 路径、hostname、machine-id 或任意 shell。退出码合同：

| Exit | 含义 | 调用方行为 |
|---|---|---|
| 0 | capability allowed | 才可进入后续已版本化步骤 |
| 20 | role 明确拒绝 | 不 mutation；记录 deny |
| 30 | request/profile/schema/catalog/owner/mode 无效 | fail closed，升级平台维护 |
| 40 | hostname 或 machine-id 不匹配 | fail closed，重新核对 machine identity |
| 64 | CLI usage error | 修复调用脚本，不重试 mutation |

`gitea-ci` 的 `scm-ci` profile 只允许 build/test/package/publish 与批准的 CI/CD 辅助能力。
故意请求 `start/application` 两次都必须返回 20，且 listener、PID、DB 和文件前后无变化。

OrbStack machine 删除是另一层 Gate，不由 host-role guard 代替。Issue #21 已在 2026-08-04 对
`prod-sim` 完成两轮 name+ID、进程/监听/挂载/timer/数据/引用、owner disposition 与可重建核对，
随后精确删除并验证 absence 与其它正式主机/项目 health。该授权已经消费，不得重放，也不得扩展到
`gitea-ci`、AppServer、其它 VM、服务、数据库或目录。未来 VM lifecycle 必须新开 Issue，重新取得
实时证据与明确授权；历史命令只存在于 Issue #21 verification。

## 2. 踩坑集（🕳️ 编号即正文引用号）

| # | 现象 | 根因 | 对策（已固化处） |
|---|------|------|------------------|
| 1 | `next build` 挂死 20+ 分钟无输出 | `next/font/google` 构建期拉 Google Fonts，弱网 TLS 黑洞 | 字体一律 `@fontsource-variable/*` 自托管（已改） |
| 2 | 构建被杀却「成功」，制品里 `.next` 是空的 | **Next 收到 SIGTERM 以 exit 0 退出**，`bash -e` 误判 | `pack.sh` 校验 `.next/BUILD_ID`（已加） |
| 3 | 部署了新制品，跑的还是旧代码/直接 crash | **PM2 reload 不应用新配置的 cwd/script**，钉死旧 release 实路径 | 部署一律 `pm2 delete + start`（已改） |
| 4 | 部署「健康检查通过」但 pm2 显示 errored | 同端口**野生进程**替真身应答了健康检查 | 部署后断言 `pm2 jlist` 目标进程 online（已加）；别在 VM 手动 `npm start` 长跑 |
| 5 | `orb ... pkill -f "xxx"` 返回 255 且无输出 | pattern 匹配到**自己所在的 bash -c 命令行**，自杀 | 模式写 `"[x]xx"` 形式，或先 pgrep 确认 |
| 6 | pm2 重启后应用丢环境变量 | `--update-env` 用当前 shell 环境**整体替换** | 重启前必 `set -a; source /opt/*/.env; set +a` |
| 7 | 用 admin token 建 token 返回 401 | Gitea **token 管理端点只认 basic auth** | `curl -u admin:密码 -H "Sudo: 目标用户" POST .../users/<u>/tokens` |
| 8 | 某用户所有 API 403："You must change your password" | **管理员创建的用户默认 must_change_password=true** | `PATCH /api/v1/admin/users/<u>` 带 `{login_name,source_id,must_change_password:false}` |
| 9 | API 返回纯文本 "400 Bad Request" | 凭据文件里有**失败重试留下的同名脏行**，`grep\|awk` 拼出多行畸形 header | 追加凭据前先清同名行；解析后校验长度 |
| 10 | 部署迁移步骤 `database is locked` | `prisma migrate deploy` 要 SQLite **独占锁**，运行中应用持连接 | 部署顺序：**停应用 → 迁移 → 切链 → 启动**（已改）；deploy-prod.sh 同序 |
| 11 | 纯文档合并也跑了完整构建部署 | deploy-test.yml 无 paths 过滤 | 无害（顺带回归）；嫌慢可加 `paths-ignore: ["docs/**"]` |
| 12 | `sudo chmod /home/coder/agent/*.sh` 报文件不存在（文件明明在） | **通配符在调用方 shell 展开**，无权限读目标目录时静默传字面量 | glob 放进 `sudo bash -c '...'` 里执行 |
| 13 | Prisma 引擎/依赖下载慢或断 | 境外源直连 | `.npmrc` 走 Verdaccio；`PRISMA_ENGINES_MIRROR=https://registry.npmmirror.com/-/binary/prisma`（CI 已设） |
| 14 | 制品在别的机器起不来（引擎不匹配） | Prisma query engine 平台相关 | schema 已配 `binaryTargets = ["native","linux-arm64-openssl-3.0.x","debian-openssl-3.0.x"]`；新平台先 `openssl version` 对号 |
| 15 | spec/code PR **合不了**：Gitea 显示草稿、标题被加 `WIP:`、`mergeable:false` | **PR 是空的（0 文件）**——commit 落到了别的分支。人在同一个 Mac 克隆上并行开多个会话时，A 会话 `git checkout` 把 B 会话的 HEAD 一起带走（共享 checkout 的 HEAD 是全局可变状态），B 的 commit 进了 A 的分支；`sfm-spec-pr.sh` / `sfm-code-push.sh` 按 **refname** 推送，**空分支照样报 `* [new branch] … 成功`**，于是开出 0 文件 PR，Gitea 随即锁成草稿 | **排查先看文件数**：`curl .../pulls/N/files`＋`git log --oneline gitea/main..spec/N`（别先查 CI/权限/分支保护——那是这个症状最容易误入的方向）。**修复（勿用 `reset --hard`）**：`git branch -f spec/N <commit>` → `git branch --contains <commit>` 确认 → 再复位 main；`PATCH /pulls/N {"title":…}` 去掉 `WIP:` 前缀即恢复可合并。**预防**：并行时非首个会话走 worktree；commit 前 `git branch --show-current` 核对（08 §3） |
| 16 | 私有仓库 API 返回 `404` / Git 返回 `Repository not found`，Codex 误判仓库或 Issue 不存在 | 先走了匿名 API，或当前 project agent/profile 没有目标仓库 ACL；应用仓库会话还可能没有发现 AISoftPlatform skills | 先核对 exact target，按 §1.1 的 authenticated ladder 使用 broker/profile、manifest-fixed Git remote 或已登录浏览器；不扩权、不复制 token、不把匿名结果当存在性证据 |
| 17 | 新接入仓库的 Analyzer/Loop 在 profile 已建立后仍返回 private `404`，或临时用 admin 跑通后继续执行 | 仓库/agent 未进入 governance manifest，project agent 不是 exact Write，profile 复用了其它项目 token，或 protection/visibility/read-back 失败 | 先执行 §1.2 manifest validation 和 `host.access.audit`，经独立 Secret provision 后再 bind/onboarding readback；失败统一 `BLOCKED_EXTERNAL`，不得恢复 `ci-bot` 或用 admin 绕过 |
| 18 | 四个 `vm.profile.*` 全部返回 `HOST_COMMAND_FAILED`，而 `host.access.audit` / `host.onboarding.check` 均 `PASS` | **`install-host-access-broker.sh` 只在 Mac 执行过，VM 内没有 `project-profile-migration`**。broker 是在 VM 内跑该工具的（§1.0 安装面归属表），Mac 装齐不代表 VM 装了。连带后果：#61 profile 迁移从未执行，VM 停在 pre-#61 布局（profile 仍内联 `GITEA_TOKEN`、无 `token_root`/`backup_root`），陈旧令牌失效后表现为私有仓 `404`，`ANALYSIS_PROVIDER` 也与 manifest 漂移——**这些都是同一根因的下游症状，不要当成独立缺陷各修一遍** | 先跑 §1.0 的 VM 侧安装面自检；缺失就在 VM 内执行 `install-host-access-broker.sh`（幂等、不绑定凭据），再用 `vm.profile.read-back` 确认。broker 现在对该情形返回 `VM_TOOL_UNAVAILABLE` 而非 `HOST_COMMAND_FAILED`。**别从「Mac 上有这个文件」推断 VM 也有**；也别用 `install-vm.sh` 代替——它是 user-scoped，装到 `$TARGET_HOME` 而非 `/usr/local` |
| 19 | profile 迁移批次后，已迁移项目的 `aisoft-project-check --remote` 两项远程检查全 GAP（`远程配置缺失: GITEA_TOKEN`）；更换共享 `common.sh` 后，**尚未迁移**的项目 Loop 立即从 `Finished` 变失败（`BLOCKED_EXTERNAL: inline GITEA_TOKEN is forbidden`） | **共享消费者与逐项目迁移的升级顺序依赖**：#61/#70 把 profile 凭据合同前进为 `GITEA_IDENTITY`+`GITEA_TOKEN_FILE`，但消费者没扫全（#107 只修了当时报错的 `gitea-readonly.sh`，5 个工具仍只认内联）；同时共享脚本一旦收紧为「只认文件」，任何未迁移项目立刻中断——两个方向的破坏都来自**合同前进与消费者/进度不同步** | **升级顺序规则**：改共享消费者（`common.sh`、`codex/tools/*`、任何多项目复用脚本）前，先确认全部项目 profile 已迁移；做不到就让消费者**过渡期双形态**（优先 `GITEA_TOKEN_FILE`、回退内联并出弃用告警），全部迁完再收紧。#111 已抽出共享解析 `codex/agent/gitea-token.sh`（file 闸门 mode 400/600、fail closed）供工具 source，并加漂移测试 `test-gitea-token-consumers.sh` 把「只认内联的新消费者」挡在 smoke；修消费者兼容性时**先全仓扫描 `GITEA_TOKEN` 消费者**，别只修报错的那一个 |
| 20 | 仓库里明明已经有某个 typed 操作（`contract.py`、manifest、测试全绿），实际调用却返回 `BLOCKED_EXTERNAL` + `REQUEST_DENIED: requested operation is not allowlisted`，看起来像凭据或 ACL 问题。**变体（#162，更难认）**：人明明在两台都执行过重装、而且拿到了 `installed host-access-broker/v1 candidate`，症状却一模一样。**范围（#171）**：同一形态适用于**全部 installer**，不止 broker 那一个；其余五个（`install-vm.sh`、`install-host-role.sh`、`install-skills.sh`、`architecture/install.sh`、`docker-release/install.sh`）**没有 `REQUEST_DENIED` 这种统一信号**，症状按 installer 各不相同（host-role guard 用旧 capability 表、architecture lock 用旧 catalog/schema、docker-release 用旧 capability gate、skills 装旧版本、loop runtime 用旧模块），因此比 broker 那次更难归因到「源陈旧」；**#250 补记**：`sync/install.sh` 的症状显形最晚——它装的是 systemd unit 与凭据 helper，源陈旧的后果落在 VM 上此后每一次 timer 触发的同步里，而不落在安装那一次命令的输出里；**#254 补记**：`skill-for-claude/install.sh` 的症状最隐蔽——它装的是 `~/.claude/skills/` 下的 managed tree，每个声明 skill 目录都是 exact tree，源陈旧时它不只是装回旧版 SKILL.md，还会把只有新版合同才有的文件当作 unmanaged entry **删掉**，并照常打印 `Pruned N stale entries` 与全部成功行；`check-drift.sh` 随后比对的是同一份陈旧 checkout，所以两边一致、看不出区别 | **broker 的操作表是安装期固定配置**：运行中的 broker 读的是 `/usr/local/share/aisoft/host-access-broker.json`，不是仓库里的那份。新增 typed 操作后没重装，运行时的操作表仍是旧的，于是新操作「不在允许列表里」——症状与权限问题同形，很容易往凭据方向查。**变体的根因是源 checkout 陈旧**：installer 从 checkout 里的 `codex/config/host-access-broker.json` 取源，checkout 的本地分支落后 remote（PR 已合并但没 fast-forward）时，它忠实地装了那份旧 manifest 并如实报成功——`installed`／`already current (no-op)` 证明的是 installed == checkout，**不是** installed == 已合并的合同，所以幂等输出在这里是假阳性。VM 经 `/mnt/mac/` 挂载的是同一份 checkout，「两台都装」这条既有防线对它完全无效，两台一起装错。**这不是 broker installer 的特性**：仓库里每一个 installer 都从 `$ROOT`（即源 checkout）取安装源，所以 checkout 落后时它们全都会忠实地装旧内容并报成功——#171 实测其余五个脚本 `grep -c 'rev-list --count'` 全为 0，一条 staleness 判断都没有 | 在 **Mac 与 gitea-ci VM 两台**都执行 `sudo bash codex/install-host-access-broker.sh`（幂等、不复制凭据），再用 `--operation host.access.audit` 确认。#108、#115 各踩一次。**判别「装了 vs 装的是旧的」**：#162 起 installer 在安装前打印 `source commit` 与 `source operations`，直接读这两行即可；分支落后 upstream 时它 fail-closed 拒装并给出 `merge --ff-only` 补救（无 remote／detached HEAD／取不到 remote ref 时只降级为警告）。**#171 起这条防线覆盖五个 installer，#182 起补齐第六个 `docker-release/install`，#250 起补齐第七个 `sync/install`，#254 起补齐第八个 `skill-for-claude/install`——仓库里的 installer 至此全部接入，没有例外**（`docker-release/install` 此前被 Issue #65 的 evidence gate 挡住，#182 把 `codex/tests/integration/test-docker-release-v2-lifecycle-e2e.sh` 里那条 name-only pin 收窄到发布语义面并显式豁免 `install.sh`，而不是扩 allowlist；`sync/install` 被 #182 的正文列为非目标并写明「若确认同因再另开」，#182 收尾时确认同因，故切出为 #250；`skill-for-claude/install` 由 #250 收尾时确认同因、切出为 #254）：共用 `codex/lib/install-source-guard.sh` 一份实现：每个 installer 都在**第一次文件系统写入之前**打印 `source checkout` / `source commit`（含 upstream 同步状态）与一个「装的是哪一版」的可读量，并在落后 upstream 时以同样的方式 fail-closed。逐 installer 的可读量与手工核对对象：`install-host-access-broker` → `operations`，对 `/usr/local/share/aisoft/host-access-broker.json`；`install-vm` → `runtime modules` + `operations`，对 `$HOME/.local/lib/aisoft-loop/` 与 `$HOME/.local/share/aisoft/`；`install-host-role` → `capabilities`，对 `/usr/local/share/aisoft/host-capabilities.json`；`install-skills` → `skills` + `matt snapshot`，对 `$HOME/.agents/skills/` 与 `$HOME/.agents/vendor/mattpocock/current`；`architecture/install` → `catalog revision` + `components`，对 `<prefix>/share/aisoft-architecture/catalog.json`；`docker-release/install` → `matrix revision` + `schemas`，对 `/opt/aisoft-docker-release/docker-release-v1/compatibility/image-stores-v1.json` 的 `.matrix_revision` 与 `/opt/aisoft-docker-release/docker-release-v1/schema/` 下的 `*.json` 文件数；`sync/install` → `units` + `runtime scripts`，对 `/etc/systemd/system/` 下的 `aisoft-inbound-sync@.service` 与 `aisoft-inbound-sync@.timer`，以及 `/opt/aisoft-sync/` 下的 `inbound-sync.sh` 与 `git-credential-token-file.sh`（`sync/` 没有携带版本号的 JSON，所以可读量是文件计数，版本证据由 `source commit` 一行承担）；`skill-for-claude/install` → `skills` + `references`，对 `$HOME/.claude/skills/` 下声明的 skill 目录数与 `aisoft-platform/references/` 下的 `*.md` 文件数（同样没有版本号 JSON，故用文件计数；第二个量不取 SKILL.md 数是因为 installer 自身的双向 manifest 校验使它与 skill 数恒等、零信息）。任一 installer 的这两行与目标端读出的数不等，即未装上。手工核对用 `python3 -c 'import json,sys;print(len(json.load(open(sys.argv[1]))["operations"]))'` 分别读 `/usr/local/share/aisoft/host-access-broker.json` 与仓库里那份，两数不等即未装上。装之前先 `git -C <checkout> status -sb` 看有没有 `behind`。新增 typed 操作必须同步六处：`contract.py` 的 `EXPECTED_OPERATIONS`、`broker.py` dispatch、`runner.py`、`codex/config/host-access-broker.json`、`codex/runtime/tests/test_host_access.py`、以及最易漏的 `codex/tests/test-host-access-broker.sh` 的 `operation_count` 与 `[.operations[].name] \| length`——漏第六处会让 smoke 红在很靠后且该测试本身不打印任何输出。**新增 typed 参数（而不只是新增操作）还有第七处**：`test_host_access.py` 里 `test_cli_exposes_only_typed_issue_and_pull_fields` 把 `execute()` 的完整 kwargs 逐字钉死，新参数不加进去就会以「expected call not found」失败——#143 新增 `--job` 时撞到，#138 只加操作不加参数所以没碰到。**#167 起**，`codex/tools/apply-classification-labels.sh` 把这个条件直接报成 `broker-operation-missing`，detail 写明「安装期旧表、不是权限问题」并给出重装命令——从工具输出可以反查到本条 |
| 21 | 在**某个项目仓**上跑 `apply-classification-labels.sh` / `mark-completed-issues.sh`，输出一切正常、格式完好、判级值也对，但结论与 Gitea 上看到的事实不符：`--verify` 说「已投影」而 Issue 上一个 `type/*` 都没有，或说「窗口已永久关闭、判级丢失」而那条 Issue 明明投影正确 | **漏传 `--project`，工具用了硬编码默认值 `aisoft-platform`**。判级值取自 `--repo` 指向的目标仓 checkout，Issue 状态与标签却取自**平台仓的同号 Issue**，而两者从不互相核对。10 个项目里 9 个中招；`aisoft-platform` 恰好等于那个默认值，所以在平台仓上一直是对的——这是它长期隐身的原因。**假通过是致命方向**：平台仓的同号 Issue 只要恰好带 `type/platform` + `complexity/complex`（那里最常见的一对），`--verify` 就干净退 0，而 #167 的判级窗口在合并时永久关闭 | **#184 起目标由 checkout 的 Git remote 判定**，`--project` 降级为必须一致的覆盖，不一致或判不出都 fail-closed；每行输出带 `project` 与 `repository`，**先读这两个字段**再读结论。识别历史误判：拿 broker 直接读一次真相 `host-access-broker --project <项目> --operation gitea.issue.labels.read --number N`，与工具输出比对。只有 GitHub remote 的 checkout（`rsdesign-new`）判不出项目，必须显式传 `--project` |
| 22 | 两个 PR 各自 CI 全绿、零冲突干净合并，合并后 `main` 立刻红，**阻塞所有后续 PR**（任何新分支都继承这个失败）。红的通常是一条手工维护的常数断言：路由计数、条目数、错误码总数、迁移编号、显式枚举的清单长度 | **两个缺口叠在一起，缺一不成事故**。缺口①：`pull_request` 事件上 `actions/checkout` 未指定 `ref:` 时检出的是 PR 分支自己的 head，不是与 base 的合并结果——**没有任何一次 CI 跑过这两条改动合在一起的树**。两侧各自把同一个常数从 74 改成 75，在各自分支上都对；三方合并的 base 是 74、两侧文本恰好相同，git 判为「双方做了相同修改」，干净合并、零冲突标记，留下 75，而真值是 76。冲突检测比较的是**文本**，这里两侧文本相同而含义不同，所以它看不见；`mergeable: true` 只说明「能合」，不说明「合出来是对的」。缺口②：即使跑了合并预览，那次预览也是在**运行那一刻**算的，而合并发生在之后；中间别人合并进 base 之后没有任何东西让它重跑，`block_on_outdated_branch: false` 让那个**过期的绿**仍然有效。只堵①会给出更危险的错觉——以为验的是合并结果，实际验的是**某个历史时刻的**合并结果 | **缺口①（项目侧）**：参考 `templates/project/ci/`。在运行那一刻现取 base 尖端、现场做三方合并，**不要用 `refs/pull/N/merge`**——该 ref 由 Gitea 后台在计算可合并性时刷新，新鲜度不由本次 CI 运行决定，检出它可能得到对着旧 base 的结果，正是要消灭的静默假绿。合并预览拿不到时必须**明确失败**，绝不回退到 head；不要给那一步加 `continue-on-error`。改法只是在既有 job 里多一步，**workflow 名 + job key + 事件三者不变**，required context 不变，分支保护不需要任何改动。`aisoft-project-check.sh` 的 `ci-merge-preview` 会读回结论。**缺口②（平台侧）**：在 Gitea 界面把 `main` 的 `block_on_outdated_branch` 打开（broker 无 `protection.set`，只能人工操作），范围是平台仓与全部 `internal-application`；`ci-outdated-branch` 会读回。**代价是真实的**：打开后并行 PR 被迫串行化，前一条合并 → 后一条必须 rebase 或重跑才能合。处置：把长跑 PR 拆小、合并前主动 `git rebase origin/main` 再推、一批相关 PR 按依赖顺序排队而不是同时开。**串行化的成本要与「主干红、阻塞所有人」的成本放在一起比**——那一轮恰好也是主干被合红的那一轮 |

## 3. AI 辅助故障诊断

AI 可以参与平台和部署故障诊断，但先收集只读、脱敏、可复现的证据。标准故障包包含：

- Issue、PR、commit SHA、workflow/run ID 和失败步骤。
- 失败命令的退出码与最小必要日志片段。
- 当前 `/opt/*/current` 指向的 release SHA。
- PM2 目标进程状态与相关 error log。
- health endpoint 的状态码和响应摘要。
- 迁移执行状态、数据库备份是否生成、是否已经回滚。
- 与上一个成功版本的部署脚本差异。

AI 排障顺序：先读 `02` 和本册 → 只读诊断 → 给出根因与复现方法 → 在开发/测试环境修复版本化脚本 → 运行真实验证 → 走 PR。生产环境不允许 AI 临场生成命令、修改脚本、绕过健康检查或自动重试危险操作。

报告必须区分 observed、changed、verified 和 pending；无法访问的环境或未运行的命令不得写成通过。

## 4. 凭据清单（位置备忘，无明文）

| 凭据 | 存放 | 用途 |
|------|------|------|
| Gitea admin 凭据 | VM-local 受保护文件（仅 break-glass） | 不用于普通 Issue/Git/PR；只在独立批准的管理回退中使用 |
| `ci-bot` PAT | 历史对象；账号保留但 manifest collaborator 已移除 | 不再消费、复制或恢复权限 |
| platform-manager audit PAT | Mac/VM 各自 repo-external mode 600 独立文件 | 跨项目只读 settings/protection/Actions；不用于 Git |
| platform-manager mutation PAT | operator-only repo-external mode 600 独立文件 | 只运行 exact-repo reconciliation；不进入 provider/profile |
| `<project>-agent` PAT | 每项目 mode 600 独立 protected credential/profile | 只操作本项目 Issue/branch/PR，不跨项目复用 |
| Claude/Codex provider 认证 | 各 provider 自己的 HOME/config | 不复制到项目 profile、仓库、通用 env 或另一个 provider |
| 历史 `prod-sim`/rsdesign 配置引用 | 已随 Issue #21 收口 | 只在 verification 中追溯，不是当前凭据或配置入口 |

轮换要点：不得恢复或轮换 `ci-bot` 作为项目身份；manager audit、manager mutation 和每项目 PAT
分别轮换。token file 丢失不自动补发第二个未知 token，先撤销旧 token、确认 profile 引用，再用
独立授权重新生成。provider 认证只在对应 provider HOME 内轮换。

## 5. 排障决策树（部署失败时）

1. 看 Actions 日志定位失败步骤（§1 命令）
2. 构建步失败 → 🕳️ 1/2/13；打包步 → 🕳️ 2；部署步 → 🕳️ 10/3/6；健康检查步 → 🕳️ 4（查 pm2 error log）
3. pm2 errored → `sudo tail /opt/act-runner/.pm2/logs/rsdesign-new-error-2.log`
4. 修复走 PR（分支保护对所有人生效）；修完 revert 或直接下一次合并会重新部署
5. 若生产部署失败：先执行既定停止/回滚，再把脱敏故障包带回开发/测试环境复现；AI 不直接修生产机

## 6. 合并后仓库与本地收尾

- 对 manifest 中明确的单个仓库运行 `gitea-governance.sh check/apply`（#35），同时
  收敛并读回 `default_delete_branch_after_merge=true`、visibility、collaborators 和 protection；
  旧 `sync-gitea-repository-settings.sh` 只作迁移兼容，不替代完整治理检查。
- 本地清理使用 `codex/tools/aigitea-cleanup-merged.sh`。默认 dry-run，删除本地
  branch 还必须显式加 `--branches`，实际执行再加 `--apply`。
- 工具永不使用 `worktree remove --force`、`git branch -D` 或 `git clean`；
  脏、未合并、近期活跃 worktree 均跳过。macOS Bash 3.2 的空候选数组也必须安全
  继续到 branch 清理。
