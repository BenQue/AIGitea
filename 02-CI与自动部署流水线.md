# 02 · CI 与自动部署流水线

> as-built（试点建成 2026-07-11；Issue #21 于 2026-08-04 将业务 runtime 迁至独立 AppServer 并完成 `gitea-ci` 清理）。所有工作流与脚本都在仓库 `admin/rsdesign-new` 内、**本身也走 PR 评审**。本册解释每一步为什么长这样——多数设计都对应一次真实踩坑（编号见 [06-运维手册](06-运维手册与踩坑集.md)）。

v3 边界：AI 可以参与开发/测试环境的部署流程设计、首次真实部署、调试和回滚验收；流程验收后，常规测试部署和全部生产部署只运行确定性脚本。

本册只记录已经验证的 Linux/Next.js/SQLite/PM2 试点，不是通用平台默认。尤其是历史
`deploy-test.yml` 在 `gitea-ci` 启动 `rsdesign-new` 的做法已经被 Issue #21 的主机职责合同
判定为 legacy 违规，并已在 2026-08-04 完成迁移、旧 runtime/vhost/DB 清理与 post-check；
相关命令只作历史证据，不得复制给新项目或重新在 `gitea-ci` 执行。
新流程在 `scm-ci` 只完成 checkout/build/test/package/publish，应用部署到独立
`appserver-test`。Windows 的目标应用为 IIS + ASP.NET Core + React `wwwroot` + PostgreSQL，
详细构建、制品和部署合同见 [12-Windows平台自动部署方案](12-Windows平台自动部署方案.md)；
迁移与验收分别见 [13](13-项目结果迁移与内网切换实施手册.md) 和 [14](14-Windows部署与迁移验收清单.md)。两类平台共用“PR 硬门、一次构建、不可变制品、配置外置、健康检查、备份与回滚”原则，但不能复用彼此的 PM2/SQLite 或 IIS/PostgreSQL 专属命令。

任何 workflow 或 wrapper 在 mutation 前调用固定安装路径：

```bash
/usr/local/libexec/aisoft/verify-host-role --action deploy --resource application
```

在 `role=scm-ci` 上该请求必须以退出码 `20` 拒绝；脚本不得捕获后继续。build/test/package/
artifact publish 使用 catalog 中对应的固定 action/resource，未知值、profile 权限错误、
hostname 或 machine-id 漂移都 fail closed。guard 不接受任意 shell，也不替调用方执行命令。

## 0. 新 Linux 默认：Docker-first release contract

新 Linux 项目不再复制下文的 PM2/tar.gz 路径。默认合同见
[docker-release/README.md](docker-release/README.md)：

```text
Gitea main merge SHA
→ 受控 linux/amd64 builder 一次 build/test
→ docker-release/v2 release.json + architecture.lock.json + Compose source/model checksums
→ image@sha256 digest publish
→ artifact-only verification（不读取 target/Docker/Compose binary）
→ target readiness → stage → migrate → activate/health
→ 人工批准后，appserver-prod 消费同一 digests/checksums
```

- `gitea-ci` 的 `scm-ci` role 只允许 checkout/build/test/package/publish/verify，不允许
  `deploy`、migration 或业务容器 start；业务 runtime 位于独立 test/prod AppServer。
- Registry transport 只按 digest pull，验证 `RepoDigests`/image ID 后创建 release-scoped runtime
  tag；V2 offline producer 按同一 tag save，consumer 在 `docker image load` 前验证 manifest、
  Compose、architecture lock、inventory、archive SHA256 和 tar member/reference allowlist，load 后
  按 runtime tag 验证 image ID/platform，不依赖 `RepoDigests`。
- Compose 禁止 `build:`、mutable-only image、root/privileged、host namespace、Docker socket、
  非 loopback publish、任意 bind mount 和硬编码环境值；要求 read-only rootfs、明确 tmpfs/命名
  卷、`cap_drop: ALL`、`no-new-privileges`、资源/日志限制、网络分区、health 和 release labels。
- 目标机不执行 `docker build`、`npm install`、`git pull` 或公网下载。Secret 只在受保护的
  外置 env file；manifest/state/argv/log 不保存 Secret 值。
- 同 SHA 且 exact healthy 是 no-op；migration identity 一次性记录；health failure 回切上一
  container release，显式 rollback 不运行 migration，PostgreSQL restore 始终是独立人工 Gate。
- Engine/Compose/image store 在 mutation 前按 committed compatibility matrix fail closed；Engine
  major 不代表 store。`containerd` 只认官方 `DriverStatus` snapshotter marker，classic 没有同级
  真实 E2E evidence 时保持 rejected。Compose 固定 `--pull never --no-build`。
- 各 target action 由 fixed `action + target_id + full SHA` grant 独立授权；stage 不读取 database
  Secret、不运行 migration/up，migrate 不 stage/up，activate 不 stage/migrate。Legacy `deploy`
  仅作为兼容 orchestration，不替代逐项授权。

下文 §1–§7 保留为 `rsdesign-new` PM2/SQLite as-built legacy adapter 的真实证据。不得把它
改写成 Docker 已部署，也不得在应用独立迁移验收前强制退出 PM2。

## 1. 目标应用的真实形态（决定一切口径）

| 事实 | 影响 |
|------|------|
| Next.js 14 单体全栈（App Router），**不是** backend/frontend 分离 | 命令全在仓库根：`npm ci` / `npm test` / `npm run build`；Nginx 纯反代无静态分流 |
| SQLite + Prisma（`DATABASE_URL` 支持覆盖，默认 `file:../storage/db/app.db` 相对 schema 解析） | 部署脚本一整套 SQLite 特有约束（见 §4） |
| 端口写死 3100；`"type":"module"` | PM2 配置必须 `ecosystem.config.cjs` |
| 包管理 npm（独立仓库有自己的 package-lock.json） | CI 用 `npm ci`；Verdaccio 缓存 |
| 字体 `@fontsource-variable/*` 自托管 | **构建零外网依赖**（🕳️ #1：next/font/google 会在构建期拉 Google Fonts 挂死） |

## 2. 两条工作流

### `.gitea/workflows/ci.yml` —— PR 检查（分支保护的硬门）

```
PR → npm ci → prisma generate → 空库 migrate deploy → npm test(Vitest 串行) → next build
```

- 状态 context 为 **`CI / test (pull_request)`**，分支保护按此名放行合并——改 workflow/job 名会连带影响保护规则，慎改。
- 空库迁移 + 测试：service 层测试直连 SQLite，`rm -f storage/db/app.db*` 后 `migrate deploy` 重建，保证每次可重现。
- docs-only 的 PR 也跑全量（顺带回归）。

### `.gitea/workflows/deploy-test.yml` —— 历史合并 main 同机部署

```
push(main) → npm ci → generate → build → pack.sh <sha> → deploy-local-test.sh <sha> → health-check.sh
```

这是 `rsdesign-new` 试点的 as-built，不是当前平台允许的新目标。迁移后的应用仓 workflow
应在 `scm-ci` 构建并发布带 checksum 的制品，再由独立、受保护的 AppServer 部署入口验证
`role=appserver-test` 后执行 deploy；对应改动必须在该应用自己的 Issue/分支/PR 中完成。

## 3. 制品（scripts/pack.sh）

```
rsdesign-new-<sha>.tar.gz  (~160MB,存 /opt/artifacts/)
├── app/
│   ├── .next/            # 构建产物(已剔除 .next/cache)
│   ├── node_modules/     # 仅生产依赖(npm ci --omit=dev;prisma CLI 在 dependencies,离线可迁移)
│   ├── prisma/           # schema + migrations + enable-wal.sql
│   ├── package.json / package-lock.json / next.config.mjs
└── ecosystem.config.cjs
```

关键守门：**打包前校验 `.next/BUILD_ID` 存在**（🕳️ #2：Next 收到 SIGTERM 会以 exit 0 退出，`bash -e` 的 CI 步骤误判成功，半成品混入制品）。

`/opt/artifacts` 只是 legacy staging，不等于可以按名称或年龄清理。retention inventory 必须
使用项目 allowlist、完整 commit SHA、计算得到的 checksum、当前 release/rollback/workflow
引用、最小保留数量与期限；默认只输出 dry-run/audit ledger。任何删除仍是独立人工 Gate。

> 为什么不用 Next standalone：`prisma migrate deploy` 在离线机需要完整 prisma CLI，standalone 裁剪后还得单独拼装迁移工具链；SQLite 单机场景制品体积不敏感，全量 node_modules 简单可靠。可作后续优化项。

## 4. Legacy 部署（scripts/deploy-local-test.sh）—— SQLite 约束全在这

```mermaid
flowchart LR
    A["解压制品到<br/>releases/&lt;sha&gt;"] --> B["source /opt/rsdesign-test/.env<br/>(注入 DATABASE_URL 等)"]
    B --> C["pm2 delete rsdesign-new<br/>⚠️ 先停应用"]
    C --> D["prisma migrate deploy<br/>+ WAL pragma(幂等)"]
    D --> E["ln -sfn → current"]
    E --> F["pm2 start(不用 reload)"]
    F --> G["sleep 5 → 断言 pm2 online"]
    G --> H["health-check /api/health<br/>(15×2s)"]
```

迁移到 AppServer 时保留以下 SQLite 顺序，但在读取 `.env`、停止进程、迁移数据库、切换
symlink 或启动 PM2 之前，root-owned wrapper 必须先验证 `deploy/application`、
`use/business-database`、`run/migration`、`stop/application` 和 `start/application` 等固定
capability。`scm-ci` 不能通过这些检查；不能以“runner 过去能执行”为兼容理由绕过。

每步的「为什么」：

| 步骤 | 原因（实证编号见运维手册） |
|------|------|
| SQLite 库在 `shared/db/app.db`（releases 之外），`.env` 用**绝对** `file:` 路径 | 库随版本切换不丢；相对路径会解析到 release 内 |
| **先停应用再迁移** | 🕳️ #10：`migrate deploy` 要独占锁，运行中应用持连接 → `database is locked`。停机窗口 = 迁移+启动，秒级 |
| `pm2 delete + start`，不用 reload | 🕳️ #3：PM2 reload **不应用**新配置里的 cwd/script，会钉死旧 release |
| 部署后断言 `pm2 jlist` 里进程 `online` | 🕳️ #4：健康检查可能被同端口野生进程骗过（发生过） |
| 迁移后执行 `enable-wal.sql`（幂等） | WAL 模式提升读写并发；注意 WAL 下备份要用 `sqlite3 .backup` 而非 cp |
| 单实例 fork（不用 cluster） | SQLite 单写者约束；「零停机」让位于确定性 |

历史环境文件 `/opt/rsdesign-test/.env` 已随 Issue #21 旧 runtime 清理；其字段设计只保留在对应
Change evidence 中，不是当前 `gitea-ci` 配置入口。现有应用配置必须从各自 AppServer profile 和
部署合同解析，不得按本段重建。

## 5. 回滚

| 层面 | 方式 |
|------|------|
| 应用 | `releases/` 按 sha 多版本保留：`ln -sfn releases/<旧sha> current && pm2 delete rsdesign-new && pm2 start current/ecosystem.config.cjs --env production` |
| 数据库 | 迁移前备份（`sqlite3 app.db ".backup <file>"`）；迁移设计要求向后兼容（先加列不删列，下线分两步） |
| 流程 | Gitea revert PR → 触发正常部署流程 |

## 6. 历史 `prod-sim` promote（已退役）

`scripts/promote-to-prod.sh <sha>` 曾设计为以 gitea-runner 身份在 gitea-ci 上手动运行，
rsync 制品到 `deployer@prod-sim.orb.local:/opt/incoming/`，再远程执行生产脚本。

**最终状态（2026-08-04）**：Issue #21 对 name+ID、依赖、唯一数据、owner disposition 与可重建
基线完成两轮核对后，只对精确目标执行 `orb delete --force prod-sim`；删除后 absence 与
`gitea-ci`/AppServer paired health 均通过。这个脚本不得再触发，也不能继续作为“下一个待办”。
应用仓中的 stale target 只能在自己的 Issue/PR 中删除或替换；新生产目标必须有
`appserver-prod` profile、不可变制品和独立审批合同。最终证据见 Issue #21 verification。

## 7. 首次部署与部署变更验收

CI、制品、迁移、健康检查或回滚属于 complex 变更，必须有 Issue 和映射的 `summary-*`、`spec-*`、`plan-*`、`verification-*` 文档。Issue #75 后新 Change 使用 readable branch/directory；已有固定数字 basename 继续按证据驱动的 legacy 合同读取，不做批量重命名。

建设期允许人在 AI 协助下执行开发/测试环境的真实命令，直到所有有效操作固化为版本化脚本。进入生产前至少验证：

- 从可重复的干净状态执行完整脚本成功。
- 连续执行两次，确认重复运行不会破坏环境或数据。
- 构建制品带 commit SHA，测试和生产使用相同字节。
- 环境变量和凭据未进入制品或日志。
- 迁移前备份可恢复。
- PM2 online 断言和 HTTP health check 都生效。
- 故意制造一次迁移、启动或健康检查失败，并验证停止/回滚路径。
- `bash -n`、ShellCheck（若可用）和部署 smoke test 有真实结果。

映射的 `verification-*` 记录执行环境、命令、结果、失败演练、回滚结果和遗留风险。AI 手工协助部署成功不等于验收；最终必须证明不调用模型也能只靠脚本完成。

生产部署失败时，脚本先停止并执行既定回滚。AI 只能读取脱敏证据，在开发/测试环境复现和修复版本化脚本，经 PR、CI 和非生产验收后再由人重新触发生产脚本。

## 8. 观察一次部署

```bash
# Actions/CI 状态——正常入口是 host access broker 的 typed 只读操作(06 §1.0)，
# 不再在 argv/管道中拼装 raw admin token：
/usr/local/libexec/aisoft/host-access-broker \
  --project <manifest-project> --operation gitea.commit.status.read ...
# 无 broker 的受限环境按 06 §1.1 的 authenticated ladder 走只读 helper。
# 日志落盘(zstd 压缩，VM 内本地文件)
sudo zstd -dc /var/lib/gitea/data/actions_log/admin/rsdesign-new/<xx>/<id>.log.zst | tail -50
```

迁移后 `rsdesign-new` 的当前测试部署入口以应用仓库 `main` 上的 workflow 与其 AppServer 侧
profile 为准；本册 §1–§7 只保留 `gitea-ci` 时期的历史证据。

## 9. 部署成功后的 Issue 记账

目标应用可在健康检查成功后调用版本化
`codex/tools/mark-deployed-issues.sh`。工具读取 merge message 中每个独立成行的
`Closes #N`，保留 type、complexity 与非生命周期标签，仅把生命周期推进为
`deployed`。回写是 best-effort：失败必须告警，但不能把已经成功的应用部署改判
失败。每个应用仓库须通过自己的 Issue/PR 接入，平台仓库不直接替它修改 workflow。

没有这条链路的仓库不会有人写 `deployed`。它们在 `codex/config/gitea-governance.json`
里声明 `deployment_lifecycle: none`，其 Issue 的唯一终态是 `completed`，
由合并后显式运行的 `mark-completed-issues.sh` 推进（`03` §11，#163）。

「哪些标签属于生命周期」由 canonical label manifest 派生，工具不再自带一份名单
（#115 AC-7）。因此除 `gitea-token.sh` 外，它还需要能读到
`gitea-label-manifest.sh` 与 `gitea-labels.json`：从仓库 checkout 调用时自动满足；
若把工具单独拷到扁平目录，这两个文件必须一并拷到同目录，否则工具会告警跳过回写
（部署仍判成功）。
