# 阶段 2.6 起步清单：Spec 驱动闭环（Claude 版，独立 spec PR）

> 建立在[阶段 2.5](#) 之上（ci-bot、coder 用户、`~/.agent.env`、systemd 定时器沿用）。
>
> **本阶段决策（已定）**：
> - 先**统一用 Claude Code** 跑全流程，端到端测通；之后再加 Codex 版。
> - 最终分工：**Claude Code 出方案（spec/plan），Codex 改代码执行**——本清单在「执行」环节预留了 Codex 接缝（[Part H](#part-h--预留将来把执行换成-codex)）。
> - **spec/plan 走独立 PR，由 Claude Code 完成**；批准（合并 spec PR）后才进入执行。
>
> **交付定位**：这是一份交给 **Claude Code（在 gitea-ci VM 内）实际执行与验证**的交接件。设计侧到此为止，落地测试转 Claude Code。用法见[末尾"交给 Claude Code 执行"](#-交给-claude-code-执行)。
>
> 试点仓库：`rsdesign-new`。下文以其为例；`<owner>` = 你的 Gitea 用户名。

---

## 全景：谁做什么（含独立 spec PR）

```
用户提 issue
   │  ①【人工/触发】打标签 needs-analysis
   ▼
poller → analyze.sh（Claude Code 只读）
   │  产出 docs/changes/<N>/00-summary.md（推到 spec/<N> 分支）+ 贴评论
   │  标签 → awaiting-triage
   ▼  ②【闸门A】你确认方向 → 打 spec-drafting
   │
你在 VM 交互式跑 Claude Code + superpowers（spec-start.sh <N>）
   │  brainstorm → 01-spec.md → 02-plan.md（提交到 spec/<N>）
   │  spec-pr.sh <N>：开【独立 docs-only spec PR】(base main)  标签 → spec-review
   ▼  ③【闸门B】你审 spec PR → **合并** → 打 approved     ← spec/plan 进 main
   │
poller → implement.sh（读 main 上的 spec/plan 实现；将来换 Codex）
   │  在 change/<N> 分支改代码 → npm test → 开【code PR】(Closes #N)  标签 → pr-open
   ▼  ④【闸门C】你审 code PR → 合并
   │
合并触发部署（阶段 2 流水线）→ 回帖 issue → 标签 deployed
```

三道人工闸门：**确认方向 / 合并 spec PR / 合并 code PR**。spec 阶段是交互式（人 + superpowers 深度参与），analyze 和 implement 是无人值守由 poller 触发。

标签中**只有 `needs-analysis` 和 `approved` 触发 poller**；其余是状态标记或人工动作提示。

---

## Part A — 仓库准备（一次性）

在 VM 的 `~/work/rsdesign-new` 克隆里操作。

### A.1 规范文件：AGENTS.md（单一来源）+ CLAUDE.md（导入）

```bash
cat > AGENTS.md <<'MD'
# AGENTS.md · rsdesign-new

## 命令
- 安装: 前后端各自目录 npm ci
- 后端测试: cd backend && npm test
- 前端构建: cd frontend && npm run build
- 生成迁移: cd backend && npx prisma migrate dev --name <名>

## 硬性规范
- 每个改动对应一个 issue；docs/changes/<N>/ 下必须有 01-spec.md 与 02-plan.md
- 改动必须带/更新测试；提交前测试必须通过
- schema 变更走 Prisma 迁移，向后兼容（先加列不删列，下线分两步）
- 只做与当前 issue 相关的最小改动；不碰 .gitea/workflows、scripts、部署文件
- 前端：企业级 B2B 风格（表格可读性优先、信息密度适中、少动画、少鲜艳色）
- SAP BOM 走 RFC 接口；PDF 图纸按 SAP 号规则从文件服务器取
- 不提交任何密钥；配置从 .env 读取

## 分支 / 提交
- spec 分支: spec/<N>   代码分支: change/<N>
- 提交: feat: <简述> (#N)
MD

# CLAUDE.md 只导入 AGENTS.md，规范单一来源
printf '@AGENTS.md\n' > CLAUDE.md

git add AGENTS.md CLAUDE.md && git commit -m "docs: AGENTS.md conventions (+CLAUDE import)"
```

### A.2 变更文档模板

```bash
mkdir -p docs/changes/_template
cat > docs/changes/_template/00-summary.md <<'MD'
---
issue: <N>
gitea_url: <url>
type: change-request        # bug | feature | change-request
title: <标题>
status: analyzed            # analyzed | spec-review | approved | in-progress | pr-open | merged | deployed
branch: spec/<N>
created: <YYYY-MM-DD>
updated: <YYYY-MM-DD>
---

## 问题/需求总结

## 影响范围

## 初步方案与建议

## 风险

## 是否需要完整 spec
MD

cat > docs/changes/_template/01-spec.md <<'MD'
---
issue: <N>
title: <标题>
status: spec-review
---

## 目标 / 为什么

## 验收标准（可测）

## 接口 / 数据模型影响（含 SAP RFC / PDF）

## 非目标（明确不做什么）
MD

cat > docs/changes/_template/02-plan.md <<'MD'
---
issue: <N>
title: <标题>
status: spec-review
---

## 任务分解

## 涉及文件

## 数据库迁移（Prisma）

## 测试计划
MD

git add docs/changes/_template && git commit -m "docs: change templates"
git push
```

### A.3 建标签

**【手动】** 仓库 Issues → Labels，新建：`needs-analysis`、`awaiting-triage`、`spec-drafting`、`spec-review`、`approved`、`pr-open`、`deployed`。

### A.4 VM 内 Claude Code 装 superpowers

```bash
# 在 VM 里启动一次 claude，进入后执行两条 slash 命令：
claude
# /plugin marketplace add obra/superpowers-marketplace
# /plugin install superpowers@superpowers-marketplace
# 装完退出。之后描述需求时 brainstorm→spec→plan 会自动激活。
```

---

## Part B — analyze.sh：产出总结（无人值守，Claude 只读）

在 `~/agent/` 下（替换阶段 2.5 的 analyze.sh）：

```bash
cat > ~/agent/analyze.sh <<'SH'
#!/usr/bin/env bash
set -euo pipefail
source "$HOME/.agent.env"
N="$1"
API="$GITEA_URL/api/v1/repos/$GITEA_OWNER/$GITEA_REPO"
AUTH="Authorization: token $GITEA_TOKEN"
label_id(){ curl -fsS -H "$AUTH" "$API/labels" | jq -r --arg n "$1" '.[]|select(.name==$n)|.id'; }

ISSUE=$(curl -fsS -H "$AUTH" "$API/issues/$N")
TITLE=$(echo "$ISSUE"|jq -r .title); BODY=$(echo "$ISSUE"|jq -r .body)

cd "$HOME/work/$GITEA_REPO"
git checkout -q main && git pull -q
BR="spec/$N"; git checkout -qB "$BR"
DIR="docs/changes/$N"; mkdir -p "$DIR"

SUMMARY=$(claude -p "读仓库结构与 AGENTS.md。为 issue #$N 写：问题/需求总结、影响范围、初步方案与建议、风险、是否需要完整 spec。只输出 markdown 正文，不要改文件。

#$N $TITLE

$BODY" --permission-mode dontAsk --allowedTools "Read,Glob,Grep" --model "$CLAUDE_MODEL")

cat > "$DIR/00-summary.md" <<EOF
---
issue: $N
gitea_url: $GITEA_URL/$GITEA_OWNER/$GITEA_REPO/issues/$N
type: change-request
title: $TITLE
status: analyzed
branch: $BR
created: $(date +%F)
updated: $(date +%F)
---

$SUMMARY
EOF

git add "$DIR/00-summary.md"
git commit -q -m "docs: analysis summary for #$N"
git push -q -u origin "$BR"

LINK="$GITEA_URL/$GITEA_OWNER/$GITEA_REPO/src/branch/$BR/$DIR/00-summary.md"
jq -n --arg b "🤖 **问题/需求总结与初步评估** → [$DIR/00-summary.md]($LINK)

$SUMMARY

---
确认方向请打标签 \`spec-drafting\`，然后在 VM 里跑 \`spec-start.sh $N\` 进入 spec 阶段。" '{body:$b}' \
 | curl -fsS -X POST -H "$AUTH" -H "Content-Type: application/json" -d @- "$API/issues/$N/comments" >/dev/null

curl -fsS -X DELETE -H "$AUTH" "$API/issues/$N/labels/$(label_id needs-analysis)" >/dev/null || true
curl -fsS -X POST -H "$AUTH" -H "Content-Type: application/json" -d "{\"labels\":[$(label_id awaiting-triage)]}" "$API/issues/$N/labels" >/dev/null
echo "已分析 #$N（分支 $BR）"
SH
chmod +x ~/agent/analyze.sh
```

---

## Part C — Spec 阶段：交互式 Claude Code + superpowers

**【闸门A 之后】** 你打了 `spec-drafting`，在 VM 里跑 helper 进入交互式会话：

```bash
cat > ~/agent/spec-start.sh <<'SH'
#!/usr/bin/env bash
set -euo pipefail
source "$HOME/.agent.env"
N="$1"
cd "$HOME/work/$GITEA_REPO"
git fetch -q origin
git checkout "spec/$N" 2>/dev/null || git checkout -qB "spec/$N" "origin/spec/$N"
echo "进入 issue #$N 的 spec 阶段（superpowers 会自动引导 brainstorm→spec→plan）"
claude "我们为 issue #$N 写 spec 和 plan。已有 docs/changes/$N/00-summary.md。请用 superpowers 的 brainstorm→spec→plan 流程，产出 docs/changes/$N/01-spec.md 和 02-plan.md（参照 docs/changes/_template，带 front-matter）。先和我 brainstorm，不要急着写。"
SH
chmod +x ~/agent/spec-start.sh
```

跑法：`source ~/.agent.env && ~/agent/spec-start.sh <N>`。你和 Claude Code 交互完成 brainstorm、逐块确认 spec、再生成 plan。**这是最关键的一道，慢一点没关系。**

完成后开**独立 docs-only spec PR**：

```bash
cat > ~/agent/spec-pr.sh <<'SH'
#!/usr/bin/env bash
set -euo pipefail
source "$HOME/.agent.env"
N="$1"
API="$GITEA_URL/api/v1/repos/$GITEA_OWNER/$GITEA_REPO"
AUTH="Authorization: token $GITEA_TOKEN"
label_id(){ curl -fsS -H "$AUTH" "$API/labels" | jq -r --arg n "$1" '.[]|select(.name==$n)|.id'; }

cd "$HOME/work/$GITEA_REPO"
git add "docs/changes/$N"
git commit -q -m "docs: spec & plan for #$N" || echo "（无新变更，可能已提交）"
git push -q -u origin "spec/$N"

# 注意：spec PR 用 Refs（不 Closes），合并它不关闭 issue
jq -n --arg t "spec: #$N spec & plan" --arg b "issue #$N 的 spec 与 plan（仅文档）。审批合并后进入执行。

Refs #$N" \
  '{title:$t, body:$b, head:("spec/"+env.N), base:"main"}' N="$N" \
 | curl -fsS -X POST -H "$AUTH" -H "Content-Type: application/json" -d @- "$API/pulls" >/dev/null

curl -fsS -X DELETE -H "$AUTH" "$API/issues/$N/labels/$(label_id spec-drafting)" >/dev/null || true
curl -fsS -X POST -H "$AUTH" -H "Content-Type: application/json" -d "{\"labels\":[$(label_id spec-review)]}" "$API/issues/$N/labels" >/dev/null
echo "已开 spec PR（spec/$N → main）"
SH
chmod +x ~/agent/spec-pr.sh
```

> `jq` 里用 `env.N` 拿分支名，避免 shell 拼接。跑法：`~/agent/spec-pr.sh <N>`。

---

## Part D — 批准 spec PR（人工闸门 B）

**【手动】** 在 Gitea 审这个 docs-only spec PR——重点看 spec 的验收标准和 plan 的任务分解是否靠谱。满意就 **Merge**（spec/plan 进 main），然后把 issue 标签改成 **`approved`**。

> 一次动作即可：合并 spec PR + 打 `approved`。`approved` 会被 poller 捞去执行。

---

## Part E — implement.sh：读 main 上的 spec/plan 执行（Codex 接缝）

```bash
cat > ~/agent/implement.sh <<'SH'
#!/usr/bin/env bash
set -euo pipefail
source "$HOME/.agent.env"
N="$1"
API="$GITEA_URL/api/v1/repos/$GITEA_OWNER/$GITEA_REPO"
AUTH="Authorization: token $GITEA_TOKEN"
label_id(){ curl -fsS -H "$AUTH" "$API/labels" | jq -r --arg n "$1" '.[]|select(.name==$n)|.id'; }
fail(){ jq -n --arg b "🤖 执行失败：$1，转人工。" '{body:$b}' | curl -fsS -X POST -H "$AUTH" -H "Content-Type: application/json" -d @- "$API/issues/$N/comments" >/dev/null || true
        curl -fsS -X DELETE -H "$AUTH" "$API/issues/$N/labels/$(label_id approved)" >/dev/null || true; exit 1; }

TITLE=$(curl -fsS -H "$AUTH" "$API/issues/$N" | jq -r .title)
cd "$HOME/work/$GITEA_REPO"
git checkout -q main && git pull -q          # main 已含 docs/changes/$N/（spec PR 已合并）
[ -f "docs/changes/$N/01-spec.md" ] || fail "main 上找不到 spec，先合并 spec PR"
git checkout -qB "change/$N"

# ===== 执行 agent（当前：Claude Code；将来在此换成 Codex —— 见 Part H）=====
claude -p "按 docs/changes/$N/01-spec.md 和 docs/changes/$N/02-plan.md 实现 issue #$N，严格遵循 AGENTS.md。改动带/更新测试；涉及 schema 用 prisma migrate dev；完成后 cd backend && npm test 必须通过。只做与本 issue 相关的最小改动。" \
  --permission-mode bypassPermissions --model "$CLAUDE_MODEL" || fail "Claude Code 执行报错"
# ============================================================================

( cd backend && npm test ) || fail "测试未通过"
git add -A
git commit -q -m "feat: implement #$N — $TITLE" || fail "无代码改动"
git push -q -u origin "change/$N" || fail "push 失败"

jq -n --arg t "feat: #$N $TITLE" --arg b "实现 issue #$N，依据 docs/changes/$N/ 的 spec & plan。

Closes #$N" '{title:$t, body:$b, head:("change/"+env.N), base:"main"}' N="$N" \
 | curl -fsS -X POST -H "$AUTH" -H "Content-Type: application/json" -d @- "$API/pulls" >/dev/null || fail "开 PR 失败"

curl -fsS -X DELETE -H "$AUTH" "$API/issues/$N/labels/$(label_id approved)" >/dev/null || true
curl -fsS -X POST -H "$AUTH" -H "Content-Type: application/json" -d "{\"labels\":[$(label_id pr-open)]}" "$API/issues/$N/labels" >/dev/null
echo "已为 #$N 开 code PR（change/$N）"
SH
chmod +x ~/agent/implement.sh
```

---

## Part F — poll.sh 改造 + 标签接线

poller 只处理两个自动化标签；spec 阶段是交互式，不轮询：

```bash
cat > ~/agent/poll.sh <<'SH'
#!/usr/bin/env bash
set -euo pipefail
source "$HOME/.agent.env"
API="$GITEA_URL/api/v1/repos/$GITEA_OWNER/$GITEA_REPO"
AUTH="Authorization: token $GITEA_TOKEN"
list(){ curl -fsS -H "$AUTH" "$API/issues?type=issues&state=open&labels=$1" | jq -r '.[].number'; }

for N in $(list needs-analysis); do "$HOME/agent/analyze.sh"   "$N" || echo "分析 #$N 失败"; done
for N in $(list approved);       do "$HOME/agent/implement.sh" "$N" || echo "执行 #$N 失败"; done
SH
chmod +x ~/agent/poll.sh
```

systemd 定时器沿用[阶段 2.5 Part F](#)，无需改动。

---

## Part G — 端到端测试（一个真实 CR）

以项目里的"图号生成"为例：

1. **【手动】** 提 issue「图号生成规则支持多段式编码」→ 打 `needs-analysis`。
2. poller → `00-summary.md` + 评论，标签 `awaiting-triage`。
3. **【闸门A】** 你看总结、确认方向 → 打 `spec-drafting`。
4. `~/agent/spec-start.sh <N>` → 和 Claude Code + superpowers brainstorm → 生成 `01-spec.md`/`02-plan.md`。
5. `~/agent/spec-pr.sh <N>` → 开独立 spec PR，标签 `spec-review`。
6. **【闸门B】** 审 spec PR → **合并** → 打 `approved`。
7. poller → `implement.sh` 读 main 上 spec/plan → 改代码 → `npm test` → 开 code PR，标签 `pr-open`。
8. CI 绿 → **【闸门C】** 审 code PR → 合并。
9. 部署到测试环境 → 验证 → promote 上 `prod-sim` / 生产。

跑通即证明 **Claude 版 Spec 驱动闭环**成立。

---

## Part H — 预留：将来把执行换成 Codex

Claude 版测通后，加 Codex 版只需动 **Part E 里标注的那一段**（spec/plan 不变，AGENTS.md 已在、Codex 也读）：

```bash
# 在 implement.sh 里，把 claude -p ... 那段替换为 Codex CLI（本地版，能连 .orb.local）：
codex exec "按 docs/changes/$N/01-spec.md 和 02-plan.md 实现 issue #$N，遵循 AGENTS.md，改动带测试，完成后跑 cd backend && npm test。" 
# 具体命令/权限/沙盒参数以 Codex CLI 当前文档为准；认证用 Codex 自己的登录。
```

其余（分支、测试硬门、开 PR、换标签）完全复用。这样就实现你要的分工：**Claude Code 出 spec/plan，Codex 执行改代码开 PR**，两套可并行对比。等做这步时我再给你 Codex 版的精确参数。

---

## ✅ 阶段 2.6 验收清单

- [ ] `AGENTS.md`（+ `CLAUDE.md` 导入）、`docs/changes/_template/` 已提交
- [ ] 7 个标签建好；VM 内 superpowers 安装成功
- [ ] `analyze.sh` 产出 `00-summary.md` + 评论并换标签
- [ ] `spec-start.sh` 能进交互式会话，superpowers 引导出 `01/02`
- [ ] `spec-pr.sh` 开出独立 docs-only spec PR（Refs，不 Closes）
- [ ] 合并 spec PR 后 `implement.sh` 能从 main 读到 spec/plan 并开 code PR（Closes #N）
- [ ] 端到端：一个 CR 走完 分析→spec PR→批准→执行→code PR→合并→部署
- [ ] Part H 的 Codex 接缝位置明确，随时可替换

---

## 🔀 交给 Claude Code 执行

设计到此完成，落地测试转 Claude Code。两种交接方式：

1. **把本文件给 VM 里的 Claude Code**：在 `~/work/rsdesign-new` 启动 `claude`，说「按 `阶段2.6-起步清单-Spec驱动闭环-Claude版.md` 的 Part A 执行仓库准备，再依次 Part B–F 落地脚本」，让它照着建文件、写脚本。
2. **你手动照跑**：Part A–F 的命令块可直接粘贴执行，Part G 做端到端验证。

我这边继续负责设计侧：需要调整流程、打磨 `AGENTS.md`/模板、或做 Part H 的 Codex 版时，回来找我即可。实测中卡住把报错贴来，我帮你定位。

---

*仓库名、owner、token、issue 号请按实际替换。*
