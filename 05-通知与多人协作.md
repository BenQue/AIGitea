# 05 · 通知与多人协作（邮件）

> 原则：**通知层是确定性基础设施，不含 AI**。用 Gitea 内建邮件体系（成熟、随账号体系扩展），演示期用 Mailpit 捕获，内网期切公司 SMTP——应用侧零改动。

## 1. 事件覆盖（Gitea 自动发信）

| 事件 | 收件人 | 对应闸门 |
|------|--------|----------|
| issue 新评论（含 🤖 分析报告） | issue 参与者 / 订阅者 | 闸门 A 就绪提醒 |
| PR 开出 / 被指派 review | 被指派人、仓库 watcher | 闸门 B / C 就绪提醒 |
| @提及 | 被提及者 | 任意协调 |
| PR 合并、issue 关闭 | 参与者 | 结果通知 |

注意：**操作者本人不会收到自己动作的通知**（Gitea 设计，正常现象）。部署结果目前无自动通知——待办增强是「deploy 工作流回帖 issue」，回帖会自然触发本表第一行的邮件。

## 2. as-built 配置

- Mailpit：`/usr/local/bin/mailpit`，systemd，SMTP `:1025` / UI **http://gitea-ci.orb.local:8025**（捕获任意收件人，Gitea 用户邮箱可以是假地址）。
- Gitea `app.ini`：

```ini
[service]
ENABLE_NOTIFY_MAIL = true      # 不开这个,mailer 配了也不发事件通知

[mailer]
ENABLED = true
PROTOCOL = smtp
SMTP_ADDR = 127.0.0.1
SMTP_PORT = 1025
FROM = "RSDesign Gitea" <gitea@rsdesign.local>
```

改完 `sudo systemctl restart gitea`。验证：任一 issue 发条评论（用非收件人账号）→ Mailpit UI 应出现 `Re: [admin/rsdesign-new] ...`。

## 3. 切换真实 SMTP（内网 / 正式启用）

只改 `[mailer]`：

```ini
[mailer]
ENABLED = true
PROTOCOL = smtp+starttls          # 或 smtps(465)
SMTP_ADDR = smtp.公司邮服或运营商
SMTP_PORT = 587
USER = 发信账号
PASSWD = 授权码
FROM = "RSDesign Gitea" <发信账号>
```

个人邮箱可行性备忘（试点期结论）：**QQ/163 授权码方式最顺**；Gmail 需应用专用密码且国内直连 `smtp.gmail.com` 不稳；Outlook 个人版已停用基础 SMTP 认证（要 OAuth，不值得折腾）。内网正式环境 → 公司 SMTP relay，通常免认证白名单直发。

## 4. 多人协作模式

1. 给每位同事建 Gitea 账号（真实邮箱），加为仓库 Collaborator（一般 Write 即可；合并权靠分支保护 + 谁有权点 Merge 控制）。
2. 协作动词即通知：issue **assign** → 被指派人收邮件；PR **request review** → 审查人收邮件；@提及 → 定向拉人。
3. 三道闸门可以分派：闸门 A（业务方向）、闸门 B（技术方案）、闸门 C（代码质量）可由不同人守——标签 + PR review 天然支持。
4. 每人自己的通知偏好在 Gitea 个人设置里调。

## 5. 可选补充：开发机即时通知

邮件已覆盖协作场景；若还要**秒级弹窗**（如部署失败立即弹 macOS 通知），方案是 Mac launchd 定时脚本轮询 Gitea API + `terminal-notifier`——纯本地确定性脚本，设计已备案（待办清单），需要时再落地。云端定时任务（claude.ai routines 等）**够不到 `.orb.local`/内网 Gitea**，不适用。
